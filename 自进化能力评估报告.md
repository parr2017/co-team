# Co_team 自进化能力评估报告

> 评估时间：2026-09-05
> 评估对象：Co_team 多 Agent 开发智能体（server 152 测试 / web / mobile 全量代码）
> 评估方法：代码路径核实 + 仓库实际痕迹取证 + 真实运行任务（xzimxize / m3wht3qe）行为验证

---

## 一、评估结论（TL;DR）

**当前项目具备自进化的"全部物理能力"和两个低层进化回路，但尚未形成"自己发现缺陷 → 自己创建修复任务 → 自己修改自己 → 自己验证改进"的完整闭环。**

| 层级 | 能力 | 状态 |
|---|---|---|
| L1 节点级自我修复 | 测试失败自动修复循环 | ✅ 已具备 |
| L2 经验层自我改进 | 知识沉淀→检索注入→指导未来任务 | 🟡 部分具备（回路通，存量薄） |
| L3 代码层自我修改 | Agent 修改本系统自身代码 | 🟡 物理能力已证实，缺验收闭环 |
| L4 自进化治理 | 缺陷→任务自动转化、自省调度、进化度量 | ❌ 缺失 |

---

## 二、调查事实与依据

### 2.1 ✅ 已具备：节点级自我修复（L1）

**事实**：`server/src/testloop.ts` 实现了完整的"测试-解析-修复-重测"循环。

**依据**：
- `findTestFailure` 识别 pytest/vitest/jest 失败命令，`parseTestOutput` 提取失败用例与错误信息
- 修复轮独立于重试预算（`maxFixRounds=3`），`buildFixPrompt` 把解析出的失败上下文喂回 Agent 定向修复
- 测试不过 → 节点 failed → **阻止提交**（commit 仅在 success 后发生）
- 达上限 → 结构化测试报告（`result.report`：framework/attempts/failures/summary）→ 主 Agent 接管 → 仍败则升级人工
- 测试背书：`test/testFixLoop.test.ts`（含 R9 报告断言）

### 2.2 🟡 部分具备：经验层自我改进（L2）

**事实**：知识回路"沉淀→检索→注入"三环齐全，但知识存量薄、无提升机制。

**依据**：
- 沉淀：Agent 执行中可调 `write_knowledge` 工具（`tools.ts`），任务结束自动复盘写入（`orchestrator.execute` 尾部 `writeKnowledge`）
- 检索注入：`relevantKnowledge` 同时注入 planner（`planner.ts:201`）与每个 Agent 的 system prompt（`orchestrator.ts` knowledgeBlock）——**已验证真实任务命中**（m3wht3qe 的 brief 注入了历史踩坑条目）
- 薄弱点：当前知识库仅 4 条（3 条为自动复盘，1 条 Agent 主动沉淀《TS→Python 时间单位陷阱》）；知识条目无法晋升为技能；无去重/过期/置信度治理
- 本次新增的 **Skill 技能系统**（`skills.ts`）打开了"人注入技能"的通道，但 Agent 尚不能自己沉淀技能

### 2.3 🟡 物理能力已证实、验收闭环缺失：代码层自我修改（L3）

**事实**：Agent 修改本系统自身代码的**物理链路完整且已被真实任务走通**，但"改完自己必须通过自己的测试"这道门禁不存在，且曾有"声称的自我修复未持久化"的实例。

**依据**：
1. **以自身为工作区运行已被证实**：任务 xzimxize 与 m3wht3qe（「分析一下当前项目的缺陷」）均以 `D:/pxx/co-team` 为工作区执行，Agent 的 `read_file/read_dir/grep` 直接读到了 `server/src`、`mobile/src` 源码（journal 与交付报告为证）
2. **写回机制真实存在**：执行成功后 `syncToWorkspace` 把沙箱改动合回真实工作区，`gitCommit` 在仓库创建 `coteam/task-*` 分支——当前工作树正处于该分支（本报告所在提交 `1df01e6` 就提交在这条任务分支上）
3. **可执行自测**：命令白名单（config.yaml）允许 `npm/node`——Agent 理论上可以运行本项目的 152 个 vitest 用例来验证自我修改
4. **反面证据（闭环缺失的实证）**：任务 xzimxize 的节点 3 曾声称"修复了 `should_broadcast` 函数的边界缺陷"，但核查 `git diff main coteam/task-vrflx170` 等任务分支差异为 **0**，`main` 上也无对应提交——**该"修复"只存在于沙箱内，未持久化、未验证、未进入代码基线**。这正是"没有自我修改验收闭环"的直接后果
5. **缺失的闭环组件**：
   - 无"缺陷报告 → 自动创建修复任务"的转化机制（分析任务的产出是死文件，没有消费者）
   - 无"修改自身代码必须通过自身测试套件"的强制门禁（harness 的 verification 字段要求 Agent 自述验证，但不强制运行项目测试）
   - 无对自我修改的差异化保护（harness.ts/skills 等元设施与普通代码同等暴露）

### 2.4 ❌ 缺失：自进化治理（L4）

**事实**：没有任何"元循环"驱动系统持续自我完善。

**依据**：
- 分析类任务产出的缺陷清单（如 xzimxize 的 report.md）没有下游：不会被解析为结构化缺陷条目，更不会自动创建修复任务
- scheduler 只做模型调度，没有"定期自省任务"（如每周自动跑一次缺陷分析）
- 进度成果表（`GET /api/projects/:id/report`）已有 token/时长/成功失败统计，但无"缺陷密度随时间变化"等进化指标追踪
- 无 governor/meta-agent 角色负责编排"发现→修复→验证→沉淀"全循环

### 2.5 安全底座（自我修改的约束条件，已具备）

自我进化必须有安全网。以下机制已就位：
- **快照与回滚**（改进10）：task-start/decision/task-end 三级快照 + 一键回滚 + 审计日志
- **分支隔离**（改进2）：所有改动在 `coteam/*` 分支 + 沙箱中，不直接污染基线
- **人工门**：计划评审（planned 状态必须人工确认）、危险节点审批（requires_approval）
- **Harness 契约**（本次新增）：输出 schema 强校验 + verification 强制 + "错误的 success 比 failed 危害大得多"写入行为准则

---

## 三、缺口总结

| # | 缺口 | 后果 | 优先级 |
|---|---|---|---|
| G1 | 自我修改无"过自己测试"门禁 | Agent 改坏自身代码可能被标记 success（如 should_broadcast 案例：修复未持久化也无人追责） | **P0** |
| G2 | 缺陷产出无下游消费者 | 分析报告沉淀即终点，缺陷不会变成修复任务 | **P0** |
| G3 | Agent 不能自我沉淀技能 | 经验只能进知识库（弱注入），无法形成强注入的可复用技能 | P1 |
| G4 | 无自省调度与进化度量 | 系统不会主动周期性自我审视，进化无量化指标 | P1 |
| G5 | 知识库治理缺失 | 目前量小影响不大，规模化后会误导 Agent | P2 |

---

## 四、改进建议

### P0-1：自我修改门禁（Self-Modify Gate）

**方案**：orchestrator 识别"自指任务"（`workspace` 解析后等于 `PROJECT_ROOT`，或 project 绑定本仓库），对该任务的 dev/refactor 节点：
1. harness 注入强制条款：「本任务修改的是 Co_team 自身源码。交付前必须运行 `npm test`（server 目录，152 用例）并在 verification 中粘贴结果摘要；测试不过不得 status=success」
2. 执行层兜底：节点完成后若 `changes` 触及 `server/src/`，自动追加运行 `npm test` 的命令（复用现有 commands 沙箱执行），失败走既有 test-fix 闭环
3. 元设施保护：`server/src/harness.ts`、`server/src/skills.ts`、`server/src/deliverable.ts` 的修改额外标记 `requires_approval: true`（元设施自我修改必须人工审批）

**成本**：低。全部复用现有机制（harness 注入点 + commands 沙箱 + test-fix 闭环 + 审批门）。

### P0-2：缺陷 → 任务的自动转化（闭环的"合拢点"）

**方案**：
1. 定义缺陷清单的结构化产出约定：分析任务的最终 JSON 增加 `defects: [{id, severity, file, description, suggestion}]`（harness 输出契约中声明）
2. 任务完成后，若 `defects` 非空：自动写入项目 memory + 提供 `POST /api/projects/:id/defects` 查询
3. 转化入口：交付成果/缺陷报告中加「创建修复任务」按钮（桌面/移动端），预填 description（缺陷描述+文件+建议）与 project_id——**人点一下即闭环**；进一步可配置"severity=P0 自动创建任务"
4. 修复任务完成后自动回链原缺陷条目（状态：已修复/待验证）

### P1-1：Skill 自沉淀

给 Agent 增加 `write_skill` 工具（与 `write_knowledge` 对称）：Agent 在执行中发现"可复用的操作规程"时，主动沉淀为技能（写入 `skills/`，人工审批可选）。或实现"知识条目被命中 ≥N 次自动提升为技能草稿"。

### P1-2：进化度量

成果表已统计 tokens/时长/成功失败。追加进化指标：
- 缺陷密度趋势：每次缺陷分析任务的 P0/P1/P2 计数随时间序列化（存 KV）
- 修复闭环率：缺陷创建数 vs 已修复验证数
- 回归健康度：自修改任务中 `npm test` 通过率

### P1-3：自省调度

`scheduler` 增加可配置的周期任务（如每周一自动创建"项目缺陷分析"标准任务，project 绑定自身），产出经 P0-2 转化为修复任务——形成无需人推动的自省心跳。

### P2：知识库治理

去重（相似度合并）、过期标记（引用过时的踩坑）、置信度（来源 agent 的历史成功率加权）。当前知识量小，暂不紧迫。

---

## 五、结论

Co_team 已经完成了自进化的**能力建设**：它能修复自己的执行失败（L1✅）、记住经验并注入未来任务（L2🟡）、甚至物理上能修改自己的源码（L3 能力✅）——真实任务已经做到"分析自己的缺陷"和"在沙箱中修改自己的代码"。

但**闭环没有合拢**：缺陷报告不会变成修复任务（G2）、自我修改不强制通过自身测试（G1）、Agent 不能沉淀技能（G3）、没有自省心跳和度量（G4）。

**建议路径**：先落 P0-1 + P0-2（两者都复用现有机制，改动量小）——落成之后，"分析缺陷 → 自动开修复任务 → Agent 改自己代码 → 跑自己 152 个测试 → 通过才合并 → 复盘沉淀知识"的完整自进化回路即宣告贯通，L4 的自省调度与度量在此之上只是锦上添花。
