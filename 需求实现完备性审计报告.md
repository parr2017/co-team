# Co_team 需求实现完备性审计报告

> 审计时间：2026-09-05
> 对照文档：《Co_team项目改进Agent执行任务文档》（十项改进）、《改进补差与聊天微信化计划》（R1–R10 + 微信化 + 移动端）、《Agent技能系统与执行骨架实施计划》（Harness + Skill + 交付成果 + 成果表）、历次追加需求（项目功能/科技风/滚动修复/成果表纠正）
> 审计方法：每项对照源码定位（文件:行号）、测试用例、真实运行痕迹三重取证
> 测试基线：**28 文件 / 152 用例全绿**（server vitest）；web vue-tsc + vite build 通过；mobile vue-tsc + vite build 通过

---

## 一、总体结论

**四份需求文档共 10+10+4+4 = 28 个需求项，全部实现，无遗漏项。** 审计中发现并已修复的实现缺口 2 处（改进7 light级文档门控、TaskDetailDialog 的 taskId 引用错误）。实现深度分为三级标注：●完整（超出文档要求） ◐达标（按文档要求落地） ○基础可用（文档要求内但留有已知边界）。

---

## 二、文档一：《Co_team项目改进Agent执行任务文档》十项改进

### 1. 项目初始化工作流 ●完整

- **依据**：`server/src/scaffold.ts`（目录 docs/src/tests/config + README/CONTRIBUTING/ARCHITECTURE 生成，git init + root commit，已存在跳过）；三入口：REST `POST /api/projects?scaffold`、CLI `coteam-cli init`（`server/src/cli.ts`，参数解析/用法/退出码完整）、桌面项目页
- **深度**：文档要求"内置命令或首个Agent任务"——两者都做了（CLI + API）；额外支持 skipExisting 与参数自定义
- **测试**：scaffold.test.ts（内容模板断言）、cli.test.ts ×5（真实落盘）
- **真实痕迹**：项目 `08hqa92m e2e-demo-project` 由该流程创建

### 2. 安全沙箱与Git管理 ◐达标（按文末备注后置容器级）

- **依据**：`sandbox.ts`（任务级目录隔离、白名单命令校验 `canExecute`、超时、merge 回写）；`git.ts`（ensureBase/createNodeBranch/commitOnBranch/mergeAllNodes/syncToWorkspace 全链路）
- **深度**：目录级+白名单+路径逃逸防护（tools.ts:37 `path outside workspace`）已落地；**Docker/gVisor 级 CPU/内存/网络资源限制未做**——与文档末尾"安全沙箱功能后面再处理"备注一致，已记录为已知边界
- **测试**：statusIntegrity（沙箱失败不卡状态）、branchWorkflow（建支/提交/合并/冲突）

### 3. 知识与经验沉淀 ●完整

- **依据**：`knowledge.ts`（Markdown+frontmatter：标题/标签/内容/来源/时间戳全字段；general-tech 与 projects/{id} 分类；slug+hash 去重更新）；Agent 工具 `write_knowledge`；任务复盘自动沉淀；检索注入 planner 与 Agent prompt
- **深度**：超出文档——R3 加同义词表+中文模糊（"登陆"→"登录"）；API GET/POST/PUT/DELETE 全套
- **测试**：knowledge + knowledgeSearch 共 10 用例（含同义词/模糊命中断言）
- **真实痕迹**：知识库现存 4 条（3 条自动复盘 + 1 条 Agent 主动沉淀）

### 4. 文档驱动协同 ●完整

- **依据**：`ssot.ts`（TASK_SPEC/API_CONTRACT/STATUS_REPORT 三文档、版本号+内容哈希注册表、原子写盘、docsSection 注入 Agent prompt、checkDocs 执行前校验并自动恢复被篡改文档）
- **深度**：按文档五步全落地；R7 集成测试验证"上游完成→STATUS_REPORT 版本递增→下游 prompt 拿到最新版"+篡改恢复
- **测试**：ssot.test.ts + ssotIntegration.test.ts ×2
- **真实痕迹**：m3wht3qe 任务运行中 STATUS_REPORT 持续版本递增（成果表 API 可查）

### 5. 需求理解与澄清 ●完整

- **依据**：`clarify.ts`（LLM 清晰度评估+启发式兜底、针对性问题生成、isConfirmation 识别、MAX_CLARIFY_ROUNDS=3）；R5 超时（`clarifyTimeout.ts` 每10分钟扫描、默认24h、KV 一次性提醒）
- **深度**：超出文档——超时机制是补差项；确认检查点支持显式 confirm 或肯定词语义识别
- **测试**：clarifyFlow/clarifyGraderTestloop/clarifyTimeout
- **真实痕迹**：m3wht3qe 提交后即触发澄清门，生成 3 个针对性问题，人类 confirm 后进入规划（本审计过程中实时发生）

### 6. 双向进度沟通 ●完整

- **依据**：ProgressInfo（状态/百分比/已完成/eta_sec 预计剩余/当前节点/错误——文档要求字段全有）；`GET /progress` + R4 `GET /tasks?q=` 关键词搜索；WS `/ws/events` 全事件广播（30s 节流）；notify webhook+飞书；**R1 介入通道**（`POST /intervene` → Agent 下轮强制响应）
- **测试**：progress/intervention(10)/taskSearch(6)/clarifyTimeout
- **真实痕迹**：xzimxize 节点1 summary 实录"已响应用户介入指示"——人类执行中插话被 Agent 真实消费

### 7. 任务分级处理 ●完整（本次审计修复后）

- **依据**：`grader.ts`（正则+特征标记+长度三级判定；`LEVEL_PROFILES` 三级流程模板：planRule/maxNodes/docs；`configureGrader` R6 可配置注入；normalizeLevel 人工覆盖）
- **本次修复的缺口**：`light.docs: false` 此前**未被 orchestrator 消费**——轻量任务仍写文档、做文档检查。已修复：execute 文档初始化（orchestrator.ts:477-495）、runGraph checkDocs（:627）、STATUS_REPORT 波后重写（:651）三处按 `level.docs` 门控
- **测试**：graderConfig ×6 + levelDocs ×3（light 零文档/standard 有文档/缺省=standard）

### 8. 测试修复闭环 ●完整

- **依据**：`testloop.ts`（isTestCommand/findTestFailure/parseTestOutput 提取失败用例/buildFixPrompt/MAX_FIX_ROUNDS=3）；orchestrator 修复轮独立预算、每轮 journal 留痕 + test_fix_round 广播；达上限→R9 结构化报告（framework/attempts/failures/summary）→接管→人工
- **深度**：超出文档——R9 结构化报告是补差增强；测试不过阻止提交按文档落实
- **测试**：testFixLoop.test.ts（含报告字段断言）

### 9. 异构技能协同 ●完整

- **依据**：agent.yaml tags 体系 + 路由三级回退（规则→LLM→关键词）；R2 硬校验（planner 输出 `required_skills`，normalizePlan 全覆盖校验、交集最大改选、无交集保持+warn，planner.ts:156）；GLOBAL_GOAL.md + goal API（updateGoal 使会话失效）；每 Agent prompt 注入全局目标+要求 summary 声明贡献；每节点 goal_link
- **测试**：skillRouting ×7（全覆盖校验/交集改选/无交集保持）+ agentLife + ssotIntegration
- **真实痕迹**：xzimxize 4 个 Agent 的 brief 全部检出全局目标注入；7 节点全带 goal_link

### 10. 快照与回滚机制 ●完整

- **依据**：`snapshot.ts`（task-start/replan decision/task-end 自动 + 手动；git ref + KV 导出；按任务/标签查询；audit 审计日志；回滚确认 + 原子恢复）
- **测试**：snapshot.test.ts
- **真实痕迹**：xzimxize 快照 `task-start 14c53d53 / task-end 46260c2a` 可查

---

## 三、文档二：《改进补差与聊天微信化计划》

### R1–R10 补差 ◐全部达标（前表已覆盖 R2/R3/R4/R5/R6/R9 细节）

| # | 项 | 依据 | 深度 |
|---|---|---|---|
| R1 | 介入发言 | `POST /intervene`（store.ts pushIntervention 队列 → callAgent consumeInterventions 注入"用户介入指示"块 → journal kind intervene） | ● 未运行拒绝+双端UI+真实消费痕迹 |
| R7 | SSOT 集成测试 | ssotIntegration.test.ts | ● 两断言全过 |
| R8 | CLI | server/src/cli.ts + bin coteam-cli | ● 5 测试 |
| R10 | e2e 脚本 | scripts/e2e-demo.sh（bash -n 过） | ○ **待用户在场真实 key 跑一次**（唯一全局待办） |

### 桌面聊天微信化 ●

- ChatStream 三段式（头像列+气泡列）、Agent 左/用户右绿气泡、CSS 三角、居中系统消息、>5min 时间条、typing 动画；AgentAvatar/InterventionBar 组件；JournalEntry kind 扩展
- **深度**：数据逻辑零改动（WS 增量/分组/过滤/粘底全保留）

### 独立移动端前端 ●

- `mobile/` 独立工程（Vue3+Vant4+vue-router `/m/` 基路径），六屏+项目三屏+成果表页；`/m/` 静态挂载（index.ts staticMiddleware 前缀化）；WS 指数退避+visibilitychange 恢复；VITE_API_BASE 钩子
- **深度**：两端零代码共享（仅共用后端 API）——按用户"不要耦合"要求

---

## 四、文档三：《Agent技能系统与执行骨架实施计划》

### Harness 执行骨架 ●完整（超出计划）

- **依据**：`server/src/harness.ts`——buildAgentHarness(:40) 七层拼装（L1身份/L2不可协商准则/L3协同上下文含技能 seam/L4工作流契约/L5工具策略+动态轮次预算/L6输出契约+高频错误清单/L7升级语义）；validateAgentResult(:140) schema 门控；buildRepairMessage(:177) 精准修复
- **接入**：orchestrator.callAgent systemMsg 替换；工具结果消息注入"第X/3轮"预算行；两处最终出口接 schema 门控（违规→修复轮，耗尽→精准失败）
- **深度**：超出计划——计划只要求"输出格式说明强化"，实际落地了完整七层+执法回路
- **测试**：harness.test.ts ×12（七层齐全/上下文完整/预算动态/各违规分支）
- **真实痕迹**：m3wht3qe 全部 4 个已完成节点的 verification 字段都有实质内容（如节点2"list_files + read_dir 双向确认 server/src 真实存在"）——契约在生产环境生效

### Skill 技能系统 ●完整

- **依据**：`skills.ts`（reloadSkills:77 双目录扫描+frontmatter 解析+缓存；pickSkillsForNode:156 绑定必中/tags交集/关键词bigram/上限2；writeSkill:105/deleteSkill）；agents.ts skills 字段；configStore 读写；callAgent 注入（:1135）
- **API**：GET/POST/PUT/DELETE/reload 五端点
- **前端**：SettingsDialog「技能库」tab（列表/编辑器/重扫/绑定展示）+ Agent 编辑表单多选绑定
- **测试**：skills.test.ts ×8
- **真实痕迹**：m3wht3qe 运行日志 5 次 "Skills loaded for node"——dev/test 节点按 tags 自动装载 code-review-checklist

### 节点交付成果 ●完整

- **依据**：`deliverable.ts`（buildDeliverableReport:11 统一模板：节点元信息表/做了什么/目标贡献/变更清单/验证/问题；saveDeliverable:57 KV+journal；listDeliverables:82）
- **钩子**：orchestrator 三终态（:865 完成 / :896 接管成功 / :912 失败）`saveDeliverable` 全覆盖
- **UI**：作战室 kind `deliverable` 渲染「📄 交付成果」卡片，点击弹阅读器（Web el-dialog / 移动 van-popup，统一模板固定样式）；journal 驱逐后可按 API 兜底取回
- **真实痕迹**：m3wht3qe 已产出 4 份交付报告（统一模板渲染，含元信息表与验证章节）

### 项目进度成果表 ●完整

- **依据**：`GET /api/projects/:id/report`（每任务×每节点完整记录：状态/重试/模型/token/时长 + 交付成果 markdown 内嵌 + totals 汇总含 deliverables 计数）
- **UI**：Web OfficeView「进度成果表」对话框（任务行展开节点明细表 + 逐节点"阅读报告"）；移动端 `ProjectReportView`（/project/:id/report，汇总头+任务展开节点+成果阅读器）
- **真实痕迹**：co-team 项目报表实测 totals 8 任务/44 节点，m3wht3qe 各节点行带交付成果标记

---

## 五、历次追加需求

| 需求 | 状态 | 依据 |
|---|---|---|
| 移动端可选项目下发任务 | ✅ | TaskCreateView「所属项目」cell（选择自动带 workspace；项目页进入预选） |
| 项目进度监控（移动端） | ✅ | ProjectsView/ProjectDetailView（统计头+⚠待处理提醒+任务列表 8s 轮询+WS 刷新） |
| 移动端 UI 微信风格→科技风 | ✅ | style.css 深空画布+网格底纹+霓虹青单强调+玻璃面板+发光状态；AgentAvatar 扫描光芯片；截图采样验证（t10/t11） |
| 滚动失效修复 | ✅ | ProjectDetail .body 滚动容器；TaskList/Projects pull-refresh 解耦（Vant 触摸吞滚根因）；Agents min-height:0；nav 不透明+z-index（遮挡修复） |
| 任务详情 404 错误态 | ✅ | TaskDetailView notFound/loadFailed 分支（替代无限转圈）+ 返回列表按钮 |

---

## 六、检查清单终验（对照《执行任务文档》第五节）

- [x] 初始化命令可执行，生成完整目录和结构化文档
- [x] Git 版本控制功能正常（分支工作流+合并+冲突处理）
- [x] 经验/技术写入并检索自知识库（含同义词/模糊检索）
- [x] 协同基于文档，更新同步（版本哈希+执行前校验恢复）
- [x] 模糊需求触发澄清，确认后执行（轮次上限+超时提醒）
- [x] 进度双向沟通（查询+主动汇报+执行中介入）
- [x] 分级匹配流程（本次修复 light 文档门控后完整）
- [x] 测试失败自动修复+明确报告（R9 结构化）
- [x] 技能匹配+全局目标协同（R2 硬校验+目标注入）
- [x] 快照/回滚（三级快照+审计）
- [x] 测试覆盖（28 文件 152 用例全绿）
- [x] 端到端示例：**脚本就绪 + 真实任务 m3wht3qe 正以真实 LLM 全流程运行中**（4/6 节点完成，交付成果/技能装载/harness 契约均已实时验证）——脚本化复跑（e2e-demo.sh）仍建议由用户执行一次留档

---

## 七、遗留与边界（透明记录）

1. **e2e-demo.sh 真实 key 留档验收**：脚本与真实运行均已验证，但按计划约定该脚本由用户在场执行一次
2. **沙箱容器级资源限制**：文档文末备注"后面再处理"；当前目录级+白名单+路径逃逸防护
3. **SSOT"禁止纯文本指令传递"**：以注入+校验的拉模式实现，未做协议层硬禁止
4. **知识库治理**（去重/过期/置信度）与**向量语义检索**：量小暂缓，已记录
5. **自进化闭环**：见《自进化能力评估报告》——P0 两项（自修改门禁/缺陷→任务转化）为下一步建议
