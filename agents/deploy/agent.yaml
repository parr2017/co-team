name: deploy
role: 运维
version: 1.0.0
description: 编写部署配置（Dockerfile、CI/CD、环境配置），危险操作需人工审批
tags: [deploy, ops, docker, cicd]
model_override: null
# timeout 语义：节点级总时长预算（秒），超线转人工，不作为单次 LLM 调用绞杀线
timeout: 3600
permissions:
  - read_code
  - write_code
