# Co_team 补差 + 聊天微信化 + 独立移动端前端 — 综合执行计划（v3）

> 创建：2026-09-05。本文档为唯一现行计划，取代本文件早先版本的《改进补差与聊天微信化计划》。
> **实施状态（2026-09-05）：四批全部完成。全量 120 测试绿（原 76 → 新增 44）；server tsc / web vue-tsc / mobile vue-tsc + vite build ×2 通过；`/m/` 挂载冒烟通过（302/SPA fallback/assets/intervene API/q 搜索）。**
> 已知待用户验收项：桌面微信化视觉效果（浏览器 Ctrl+F5）、移动端真机体验、R10 e2e-demo.sh 真实 key 全流程（需用户在场执行）。
> 前置：十项改进已落地（commit `46260c2`，15 文件 76 测试全绿）。
> **三大工作流**：R1–R10 补差 ＋ 桌面聊天微信化 ＋ **独立移动端前端**。
> **架构总则（用户明确要求）**：桌面 web 与移动端**完全解耦**——桌面端**零响应式改动**（不引入任何 @media/断点/全屏规则），移动端是**全新独立前端工程**，二者互不依赖，只共用后端 REST API + WS。

---

## 〇、移动端定位（用户已确认决策）

- **形态**：独立移动端 Web 工程 `mobile/`（局域网 HTTP，手机浏览器访问 `http://<电脑IP>:8855/m/`）。后续在此工程基础上用 Capacitor 打包 iOS/安卓 app；飞书 bot 等平台接入为更后置路线（本次均不实施，仅保证不设障碍）。
- **功能范围**：监控 + 轻交互 + 发起任务——任务列表/搜索/进度、作战室微信式聊天、介入发言、审批/取消、澄清问答、计划确认、发起任务、Agent 状态总览。项目管理（OfficeView/项目列表）、设置、知识库、DAG/工位大屏等低频重界面 **v1 不做**（桌面端承担）。
- **环境**：局域网 HTTP。后端已绑 `0.0.0.0:8855`、API/WS 全相对路径——移动工程同源挂载在 `/m/` 路径下，**无需 CORS、无需鉴权改动**。

---

## 一、遗漏清单（对照需求文档复查结论）

| # | 遗漏点 | 现状 |
|---|---|---|
| **R1** | 执行中消息式介入（用户反馈渠道） | ❌ 只有审批/取消/换模型按钮式介入，无"执行中发一句话给 Agent"通道 |
| **R2** | 技能标签硬校验分配 | ⚠️ 仅软匹配，normalizePlan 不校验节点技能需求 vs agent tags |
| **R3** | 知识库语义检索 | ⚠️ 仅关键词检索 |
| **R4** | 按关键词查询进度 | ⚠️ 仅按任务 ID 查 |
| **R5** | 澄清循环超时机制 | ⚠️ 有 3 轮上限，无超时提醒 |
| **R6** | 分级标准可配置 | ⚠️ 分级正则/阈值硬编码 grader.ts |
| **R7** | SSOT 多Agent集成测试 | ⚠️ 仅单元测试 |
| **R8** | 初始化封装为内置命令 | ⚠️ 仅 REST API + UI，无 CLI |
| **R9** | 结构化测试报告 | ⚠️ 仅 journal 日志，无结构化报告 |
| **R10** | 真实 LLM 端到端示例 | ❌ 未用真实模型跑通完整成功路径 |

---

## 二、R1–R10 补差实现方案

### R1 介入发言（用户消息式反馈渠道）——与微信化、移动端同批实施
**后端**（`server/`）：
- `POST /api/tasks/:taskId/intervene { message }`：
  - 消息入 KV 队列 `task:intervene:{taskId}`（数组：`{id, message, ts, consumed}`）
  - 写作战室日志（JournalEntry **kind 扩展 `'intervene'`**，role `'master'`）
  - `emitProgress('user_intervened')` 广播 + notify 通知
- `orchestrator.callAgent`：每轮构建 userMsg 前 `consumeInterventions(taskId)`：读取并清空队列，注入 userMsg 块 `## 用户介入指示（必须响应，并在汇报中说明如何落实）`；journal 留痕"介入消息已注入节点 xxx"
- 借现有 WS `journal_append` 推送，介入消息实时以绿气泡出现在聊天流（零额外前端推送逻辑）
**前端**（`web/`）：
- `InterventionBar.vue`：微信式输入条（输入框 + 绿色发送按钮 + Enter 发送），嵌入 TaskDetailDialog 作战室右侧底部；api client 加 `interveneTask`；发出后提示"将在 Agent 下一轮对话注入"
**测试**：mock chat 捕获 userMsg 含介入内容、队列消费清空、journal 留痕、未运行任务拒绝介入。
**双端消费**：桌面 InterventionBar 与移动端聊天输入条都调这一条 API。

### R2 技能标签硬校验分配
- planner 提示词要求节点输出 `required_skills: [...]`；`normalizePlan` 保留该字段
- `normalizePlan` 校验所选 agent tags 覆盖 `required_skills`；不满足自动改选 tags 交集最大的可用 agent；无交集保持原选 + warn
- 签名改传 `Map<string, {name, tags}>`
**测试**：`required_skills=['test']` 节点不会分给无 test 标签的 dev，自动改选 tester。

### R9 结构化测试报告
- 修复循环终态 `result.report = { framework, attempts, failures: [{name, message}], summary }`；成功则 failures=[]，达上限给完整明细
- `AgentResult` 加 `report?`；桌面执行详情 UI 节点 summary 下方展示报告标签
**测试**：达上限场景断言 report 存在且 failures 非空。

### R4 关键词查询
- `store.listTaskGraphsPaged` 加 `q` 参数（description/task_id 大小写不敏感模糊）；`GET /api/tasks?q=` 透传
- 桌面 TaskList 搜索框 + 移动端任务搜索共用此后端能力（**后端部分随移动批次先行**）
**测试**：store 层过滤单测。

### R5 澄清超时
- 启动定时扫描（每 10 分钟）：`status==='clarifying'` 且 `updated_at` 超阈值（config `orchestrator.clarify_timeout_hours`，默认 24h）
- 触发 notify 提醒 + `emitProgress('clarify_timeout')`；KV 标记每任务仅提醒一次
**测试**：注入超时任务 → 提醒触发且不重复。

### R6 分级标准可配置
- config.yaml 新增 `grading: { heavy: [...], light: [...] }`（可选，追加内置规则）
- `grader.configureGrader({heavy?, light?})` 启动注入，追加到内置正则；不调用行为不变
**测试**：configure 后 gradeTask 结果变化。

### R3 知识库语义检索（轻量版）
- `searchKnowledge` 增强：内置同义词表（js↔javascript、py↔python、重试↔retry、缓存↔cache、登录↔login/auth 等）+ 中文 2-gram 模糊匹配（"登陆"→"登录"类近似）；API 签名不变
- 真·向量语义检索成本高，记入后续待办
**测试**：搜 javascript 命中 js 条目；搜"登陆"命中"登录"条目。

### R7 SSOT 集成测试
- 两节点 mock 任务（n1 dev → n2 dev）：上游完成后 STATUS_REPORT 版本递增 → 断言下游 docsBlock 拿到最新版本号；篡改沙箱文档后下游执行前 checkDocs 自动恢复
**测试**：orchestrator 集成测试（复用 mock chat 模式）。

### R8 CLI 内置命令
- 新建 `server/src/cli.ts`：`coteam init <dir> [--name xxx] [--description xxx]` → 调 `scaffoldProject`
- `server/package.json` 注册 bin；README 补 CLI 用法
**测试**：参数解析 + 执行结果单测。

### R10 真实 LLM 端到端脚本
- `scripts/e2e-demo.sh`：创建项目(scaffold:true)→建任务(含 main_model_id)→轮询 progress 至 completed→输出快照/知识库/作战室摘要
- README 附使用说明；**由用户在场用真实 key 手动跑一次**做验收；`bash -n` 语法校验

---

## 三、桌面聊天界面微信化（仅 web/，与移动端无关）

### 设计规范（微信元素 → Co_team 映射）

| 微信元素 | 落地方案 |
|---|---|
| 圆角方形头像 + 昵称 | Agent 消息左侧 36px 圆角方头像，主 Agent 蓝底"主"，子 Agent 按角色配色（dev=绿 / test=黄 / review=紫 / deploy=橙 / docs=青 / refactor=粉，兜底按名字 hash 取色），头像显示名字前 2 字符；昵称灰字显示在气泡上方 |
| "我"的绿色气泡 | 用户介入消息右侧绿气泡（亮色 #95EC69 / 暗色 #3eb575），"我"头像在右 |
| 气泡小三角 | CSS 三角尾巴，左上 / 右上指向头像 |
| 居中灰色系统消息 | 工具动作行（⚙ round）改居中灰色小字样式 |
| 居中时间分隔 | 节点分组头改微信时间条样式（保留模型徽标） |
| 时间戳省略 | 仅与上一条消息间隔 >5 分钟时显示居中时间戳，气泡内不重复精确时间 |
| 文件消息卡片 | 📎 附件改微信式文件卡片（图标+名称+展开） |
| "对方正在输入…" | 左头像 + 灰气泡三点动画 + "xx 正在中"（复用现有 WS agent_activity 数据源） |
| 底部输入条 | InterventionBar：输入框 + 绿色发送按钮 + Enter 发送；提示"将在 Agent 下一轮对话注入"；发出后绿气泡即时出现 |

### 改动文件

| 文件 | 改动 |
|---|---|
| `web/src/components/ChatStream.vue` | 三段式模板（头像列+气泡列）+ `intervene` 右侧绿气泡渲染 + 微信风格样式全面重写。**数据逻辑不动**（WS 增量、节点分组、过滤、智能置底） |
| `web/src/components/AgentAvatar.vue` | **新增**：props(name/size/active)，微信化头像配色逻辑集中于此，web 内部复用 |
| `server/src/types.ts` + `web/src/api/index.ts` | JournalEntry kind 增加 `'intervene'` |
| `web/src/components/InterventionBar.vue` | 新组件：微信输入条 |
| `web/src/components/TaskDetailDialog.vue` | 作战室 wr-right 底部嵌入 InterventionBar |
| `web/src/components/ChatReplay.vue` | 自动继承 ChatStream 新样式，仅调聊天背景色（微信淡灰 / 暗色对应） |

已确认决策：Agent 全部左侧 / 用户介入右侧（微信群聊心智）；时间戳微信式省略（间隔 >5 分钟才显示）。

---

## 四、独立移动端前端（本计划核心新增）

### 4.1 技术选型

- **Vue 3 + Vite + TypeScript**；**vue-router**（移动端页面路由+物理返回键体验必需）
- **UI 库：Vant 4**（NavBar/Tabbar/List/PullRefresh/Search/Dialog/Toast/Picker 等；聊天流与气泡为自定义微信风格 CSS，不依赖 UI 库）
- 状态：沿用项目既有模式——**模块级单例 composable store**（不引 pinia，与 web/useDashboard 同风格）
- **不引 echarts**：Agent 状态用原生卡片、执行详情用轨道列表
- API 客户端：`mobile/src/api/index.ts` **独立精简版**（fetch 封装 + 所需类型子集）——与 web 的 api 模块**有意双份**，换取彻底解耦（后端 types.ts 为概念 SSOT，后续可代码生成，本次不做）

### 4.2 工程结构

```
mobile/
  index.html          # viewport-fit=cover
  vite.config.ts      # 端口 8857，/api /ws 代理到 8855（同 web 模式）
  public/             # manifest.json + 图标 + 主题色（添加到主屏幕用，无 SW——HTTP 下不可注册）
  src/
    main.ts / App.vue # 根壳：路由出口 + Tabbar
    router.ts
    api/index.ts       # 独立精简 API 客户端 + 类型
    composables/
      useDashboard.ts  # 单例 store：tasks/agents/WS 事件（复刻 web 模式，独立实现）
      useWs.ts         # WS 连接：指数退避重连 + visibilitychange 回前台立即重连+补拉
      useTheme.ts      # 深色模式，默认跟随系统 prefers-color-scheme
    components/
      AgentAvatar.vue      # 微信化头像（配色规范与桌面一致，代码独立）
      ChatStream.vue       # 微信式聊天流（移动版：全屏 flex、触摸粘底、safe-area）
      InterventionInput.vue# 底部固定输入条（safe-area-inset-bottom）
      NodeCard.vue / AgentCard.vue 等
    views/
      TaskListView.vue     # 任务列表：搜索(R4 q)+状态筛选+下拉刷新+加载更多+审批入口
      TaskDetailView.vue   # 任务详情：顶部进度条 + 三个分页（作战室/执行详情/事件）
      TaskCreateView.vue   # 发起任务：描述/分级/主模型/工作区目录(GET /api/fs 级联 Picker)
      ClarifyView.vue      # 澄清问答（全屏页，任务阻塞点刚需）
      PlanReviewView.vue   # 计划确认：节点卡片查看 + 确认启动/replan
      AgentsView.vue       # Agent 状态总览（监控）
```

路由：`/tasks`（Tabbar 任务）· `/agents`（Tabbar 状态）· `/task/new` · `/task/:id` · `/clarify/:id` · `/plan/:id`。新建任务为页面右上角/FAB 入口。全局 CSS：100dvh、input 16px（防 iOS 缩放）、safe-area、微信配色 token（亮/暗）。

### 4.3 作战室（移动端核心屏）

- 结构：NavBar（任务名+返回）→ 成员头像横向滚动条（点选过滤，微信化配色）→ **微信式聊天流**（Agent 左/用户右绿气泡/系统消息居中/时间条，样式规范与桌面微信化一致）→ **底部固定 InterventionInput**（发送 → `POST intervene` → 经 WS `journal_append` 绿气泡即时回流）
- 执行详情分页：PipelineTrack 改纵向节点卡片列表 → 点选展开 summary/报告(R9)/timeline/对话回放
- 事件分页：时间线列表
- waiting_approval 节点在详情页与列表均给出醒目审批按钮

### 4.4 后端配套改动（仅两处小改）

1. **`/m/` 静态挂载**：`server/src/index.ts` staticMiddleware 增加第二挂载点——`<root>/mobile/dist` serve 于 `/m/` 前缀（`COTEAM_MOBILE_DIST` 环境变量可覆盖，同现有 `COTEAM_WEB_DIST` 模式），`/m/` SPA fallback 到 `mobile/dist/index.html`。同源零 CORS
2. **R4 后端部分提前**（`q` 参数），移动端搜索依赖

根 package.json 增 `dev:mobile` / `build:mobile` 脚本；README 增"手机访问"小节（同 WiFi、防火墙放行 8855、访问 `http://<局域网IP>:8855/m/`）。

### 4.5 移动端测试

- vitest：composable 单测——WS 重连/visibilitychange 恢复、api 客户端、搜索过滤
- 视觉与交互由用户手机真机验收（Chrome DevTools 375px 辅助）

### 4.6 为后续 app / 飞书留门（不实施）

- **Capacitor 打 app**：独立工程 + `VITE_API_BASE` 钩子（api/ws 基址可配，默认空=同源）即绪
- **飞书 bot**：notify.ts webhook 已有（出方向）；收消息需回调端点+公网+**鉴权前置**
- **鉴权（所有对外形态的前置条件/红线）**：当前全部 API 裸奔（含模型池 api_key 读写、`GET /api/fs` 目录浏览），公网/app/飞书接入前必须先加最小 token 门禁

---

## 五、执行顺序

```
第一批：R1 后端介入通道 → 桌面微信化（AgentAvatar → ChatStream 重构 → InterventionBar）
        （桌面端独立完成，不含任何移动/响应式内容）
第二批：R4 后端 q 参数 → 移动端工程脚手架（Vant/router/主题/api/WS composable）
        → 六屏实现（任务列表/详情作战室/发起/澄清/计划/Agent总览）→ /m/ 挂载
第三批：R2 → R9 → R4 桌面搜索框 → R5
第四批：R6 → R3 → R7 → R8 → R10
收尾：全量 vitest 回归 + server tsc / web vue-tsc / mobile vue-tsc + vite build ×2
      + 手机真机验收 + 桌面零回归确认 + 文档更新（本计划、README、开发路线.md）
```

- 每项完成即跑对应测试，通过后再进入下一项
- 不主动 commit，全部完成后由用户决定提交方式
- 视觉验收：微信化效果由用户在浏览器确认（Ctrl+F5 强刷）；移动端由用户手机真机确认

## 六、验收清单（实施后状态）

**补差（R1–R10）——自动化验证已通过**：
- [x] 介入发言：API 队列/注入/journal 留痕/未运行拒绝（10 测试）；绿气泡经 WS journal_append 实时回流（待视觉确认）
- [x] required_skills 与 agent tags 不匹配时自动改选（7 测试）
- [x] 修复循环终态输出结构化 report（达上限含完整明细，escalation 后保留；2 测试）
- [x] `GET /api/tasks?q=` 关键词搜索生效（6 测试）；桌面搜索框可用
- [x] clarifying 任务超时提醒且仅一次（2 测试）
- [x] config.yaml 自定义分级关键词生效（6 测试）
- [x] 知识库同义词/模糊命中（6 测试：javascript↔js、"登陆"→"登录"）
- [x] SSOT 集成测试通过（下游拿到最新文档版本；篡改自动恢复；2 测试）
- [x] `coteam init` CLI 可用（bin `coteam-cli` + `npm run cli`；5 测试）
- [x] e2e-demo.sh 就绪（`bash -n` 通过）——**⏳ 真实 key 全流程验收待用户在场执行**

**桌面微信化**：
- [x] Agent 左/用户右、圆角方头像、气泡三角、居中系统消息、>5 分钟居中时间——代码完成，**⏳ 视觉效果待用户浏览器确认（Ctrl+F5）**
- [x] 桌面布局与功能零改动（除微信化本身；无任何响应式代码引入）

**移动端独立工程**：
- [x] `http://<IP>:8855/m/` 挂载：302→/m/、SPA fallback、assets 全 200（冒烟验证）
- [x] 任务列表/搜索/下拉刷新、作战室微信式聊天+介入输入条、审批/取消/澄清/计划确认/发起任务（含目录级联选择）、Agent 总览——代码完成，**⏳ 真机体验待用户验收**
- [x] WS 指数退避+回前台恢复（visibilitychange）
- [x] 深色模式跟随系统；manifest 添加到主屏幕
- [x] **桌面 web 全程零响应式改动**（两端完全解耦）

**收尾**：[x] 全量 120 测试回归 + server tsc / web vue-tsc / mobile vue-tsc + vite build×2 + 文档更新（README/config.yaml 示例/开发路线.md/本文档）

## 七、已知限制（记录不处理）

- 局域网 HTTP：Service Worker/推送等安全上下文 API 不可用（HTTPS/app 阶段解决）；iOS 主屏图标以 Safari 方式打开
- 移动端 v1 不含：项目管理/设置/知识库/DAG 图/指标图表（桌面承担）
- 移动端行内编辑类重交互（计划节点精细编辑）建议回桌面
- 公网/飞书/app 的一切对外路线，鉴权是硬前置
