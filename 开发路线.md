# Co-Team 开发路线图

## 项目定位

面向个人开发者的多 Agent 协作开发助手。核心理念：用户提需求 → 自动拆解任务 → 分发给专业 Agent → 实时可见 → 结果汇总。

> **当前状态（2026-09-02）**：阶段一至阶段五的核心功能已全部实现并通过测试与端到端验证；
> 阶段六产品化中与打包相关的事项待发布时完成（详见各表状态）。

---

## 阶段一：基础可用 ✅ 已完成

**目标：单机跑通核心链路，能用起来。**

| 模块 | 状态 | 原待完成项 |
|------|------|--------|
| Orchestrator | ✅ LangChain 集成 | ✅ LLM 规划 prompt 调优（含 agent 职责/记忆注入）、edges 自动生成（无效边自动回退为线性链） |
| Model Pool | ✅ 基础调度 | ✅ 模型降级策略（fallback_chain + 健康度冷却）、超时/槽位等待 |
| Plugin System | ✅ 发现 + 加载 | ✅ 动态热加载（`POST /api/agents/reload`、`coteam` CLI）、版本管理（agent.yaml `version` 字段） |
| Router | ✅ 规则匹配 | ✅ LLM 智能路由（规则 → 语义 LLM → 关键词三级回退） |
| Transport | ✅ MemoryBus | ✅ Redis 持久化（可用时自动启用）、MemoryBus 落盘（`COTEAM_STATE_FILE`）、消息确认（低优先级，暂以 Redis 自身可靠性替代） |
| Dashboard | ✅ 中文界面 | ✅ Agent 卡片交互（真实团队列表 + 实时状态）、进度条动画 |
| CLI | ✅ 基础命令 | ✅ 多轮对话（`coteam chat`）、`coteam setup` 可写入 API Key |
| API | ✅ REST + WS | ✅ 执行端点（`POST /api/tasks/{id}/execute`）、任务取消（`POST /api/tasks/{id}/cancel`）、审批（`POST /api/tasks/{id}/approve/{node}`） |

**阶段一交付物：✅ 全部达成**
- ✅ 用户可通过 CLI 或 Dashboard 提交任务
- ✅ 任务自动拆解为 DAG 并执行（LangGraph 驱动，并行 fan-out/join）
- ✅ 实时看到每个 Agent 的工作状态（WS 推送，已修复事件循环阻塞 bug）
- ✅ 执行结果和文件变更可见（任务列表/事件日志/指标面板）

---

## 阶段二：执行引擎 ✅ 已完成

**目标：Agent 真正能操作文件，不只是返回 JSON。**

| 模块 | 优先级 | 状态 |
|------|--------|------|
| Sandbox | P0 | ✅ 任务级沙箱副本，成功后 merge 回真实工作区，失败即丢弃 |
| Git 集成 | P0 | ✅ 自动创建 `coteam/task-{id}` 分支并提交变更文件 |
| Tool System | P0 | ✅ 运行中可调 read_file/list_files（tool_calls 轮次）；最终输出 files 落盘 + commands 白名单执行（含超时、路径逃逸防护） |
| Token Budget | P1 | ✅ 池级用量统计 + 单任务预算（`orchestrator.token_budget`），超限快速失败并通知 |
| 失败重试 | P1 | ✅ N 次重试（RETRYING 状态）→ 主 Agent 接管（调整策略/温度）→ 人工介入标记（needs_human + 通知） |
| Agent Output 解析 | P1 | ✅ 从 LLM 输出提取 files/commands/changes 并真实应用，代码围栏/JSON 提取容错 |

**关键设计决策：✅ 已按此流程实现**
```
Agent 执行流程：
1. 接收任务描述 + 工作目录（含现有文件清单）
2. 调用 LLM 生成执行计划（可多轮请求读取工具）
3. 通过 Tool System 执行（读文件、写文件、运行命令）
4. 验证结果（commands 运行测试/lint，失败计入 errors）
5. 返回变更列表 + 摘要
```

---

## 阶段三：专业 Agent ✅ 已完成

**目标：每个角色有专业能力，不只是通用 dev。**

| Agent | 职责 | 状态 |
|-------|------|------|
| `dev` | 开发实现 | ✅ v1.1.0（新增 files/commands/tool_calls 输出协议） |
| `review` | 代码审查 | ✅ v1.0.0（blocker 问题自动判失败） |
| `test` | 测试编写 | ✅ v1.0.0（编写并运行 pytest） |
| `deploy` | 部署配置 | ✅ v1.0.0（handler 注入 requires_approval=true） |
| `docs` | 文档编写 | ✅ v1.0.0 |
| `refactor` | 重构优化 | ✅ v1.0.0 |

**每个 Agent 包含：✅ 结构齐备**
```
agents/<name>/
├── agent.yaml      # 配置：模型偏好、超时、标签、version
├── prompt.md       # 系统提示词（含输出 JSON 契约）
├── handler.py      # 预处理/后处理钩子
├── tools/          # 工具说明（共享工具系统对接）
└── examples/       # 示例输入输出
```

---

## 阶段四：智能调度 ✅ 已完成

**目标：根据任务特点自动选择最优执行策略。**

| 功能 | 状态 |
|------|------|
| LLM 路由 | ✅ 规则匹配 → LLM 语义路由 → 关键词匹配三级回退 |
| 并行执行 | ✅ LangGraph fan-out/join（无依赖节点同超步并发），线程驱动兜底 |
| 模型降级 | ✅ fallback_chain 按优先级降级 + 连续失败健康冷却（60s） |
| 成本优化 | ✅ 节点 complexity（simple/normal/complex）→ simple 选最便宜、complex 选最强 |
| 上下文共享 | ✅ 下游节点 prompt 注入上游节点 summary + 变更清单 |
| 记忆系统 | ✅ 跨任务经验（成功/失败教训）注入规划 prompt（保留最近 20 条） |

---

## 阶段五：监控与协作 ✅ 已完成（协作模式多人共享除外）

**目标：可观测性 + 通知。**

| 功能 | 状态 |
|------|------|
| 执行日志 | ✅ 事件总线推送全部生命周期事件（含 retry/escalate/approval/cancel），Dashboard 事件日志展示 |
| 性能指标 | ✅ `GET /api/metrics`：token 消耗、成本、成功率、Agent 负载；Dashboard 侧栏指标面板 |
| 通知推送 | ✅ `src/notify`：webhook（`COTEAM_WEBHOOK`）+ 飞书（`COTEAM_FEISHU_WEBHOOK`），任务完成/失败/需审批/需人工介入均触发 |
| 审批卡片 | ✅ requires_approval 节点暂停执行 → Dashboard 一键审批 → 断点续跑 |
| Dashboard 增强 | ✅ 任务历史（含描述/状态/取消/审批操作）、模型池余量条、运行指标 |
| 协作模式 | ⏳ 多人共享任务视图（单用户定位，暂缓） |

---

## 阶段六：产品化 ⏳ 进行中

**目标：pip install 即用，零配置。**

| 功能 | 状态 |
|------|------|
| 打包分发 | ⏳ pyproject.toml 就绪，`pip install -e .` 可用；待发布 PyPI |
| 交互式配置 | ✅ `coteam setup` 引导式写入 API Key |
| 内嵌 Redis | ⏳ MemoryBus 已可无 Redis 运行并落盘；内嵌 redis-server 二进制待定 |
| 插件市场 | ⏳ 未开始（插件目录结构已标准化，具备条件） |
| 多项目支持 | ✅ 任务级 workspace 隔离（每个任务独立工作目录 + 沙箱） |
| API 文档 | ✅ FastAPI 自动 OpenAPI（`/docs`） |

---

## 阶段六.5：补差 + 聊天微信化 + 独立移动端 ✅ 已完成（2026-09-05）

**来源：《改进补差与聊天微信化计划.md》（R1–R10 遗漏补差 + 微信化 + 移动端），全量 120 测试绿。**

### R1–R10 遗漏补差

| # | 项 | 落地 |
|---|---|---|
| R1 | 执行中消息式介入 | ✅ `POST /api/tasks/:id/intervene`（KV 队列 + journal `intervene` 留痕 + `user_intervened` 广播）；orchestrator 每轮 userMsg 前消费队列注入"用户介入指示"块；未运行任务 400 拒绝 |
| R2 | 技能标签硬校验 | ✅ planner 输出 `required_skills`；normalizePlan 校验 agent tags 全覆盖，不满足改选交集最大者，无交集保持+warn（10 测试） |
| R3 | 知识库语义检索 | ✅ 14 组同义词表（js↔javascript、重试↔retry 等）+ 中文 2-gram/字符 Jaccard 模糊匹配（"登陆"命中"登录"），API 不变（6 测试） |
| R4 | 关键词查询 | ✅ `GET /api/tasks?q=`（description/task_id 不敏感模糊，兼容 project_id 过滤）；桌面 TaskList 搜索框 + 移动端搜索共用（6 测试） |
| R5 | 澄清超时提醒 | ✅ 每 10 分钟扫描 clarifying 超阈值任务（config `clarify_timeout_hours` 默认 24h），notify + `clarify_timeout` 广播，每任务仅提醒一次（2 测试） |
| R6 | 分级标准可配置 | ✅ config.yaml `grading: {heavy,light}` + `configureGrader()` 启动注入，追加内置规则（6 测试） |
| R7 | SSOT 集成测试 | ✅ 两节点 mock 任务：上游完成后 STATUS_REPORT 版本递增且下游 system prompt 拿到最新文档；篡改沙箱文档后 checkDocs 自动恢复（2 测试） |
| R8 | CLI 内置命令 | ✅ `server/src/cli.ts`：`coteam init <dir> [--name] [--description]` 调 scaffoldProject；bin `coteam-cli` + `npm run cli`（5 测试） |
| R9 | 结构化测试报告 | ✅ 修复循环终态 `result.report = {framework, attempts, failures, summary}`；达上限保留完整失败明细（含 escalation 后）；成功通过且经历修复轮时报告 attempts+零失败；桌面/移动 UI 展示 |
| R10 | 真实 LLM e2e | ✅ `scripts/e2e-demo.sh`（scaffold 建项目→建任务→轮询→快照/知识库/作战室摘要），`bash -n` 通过；**真实 key 全流程验收由用户在场执行** |

### 桌面聊天界面微信化

- ChatStream 全面重写：Agent 左侧（36px 圆角方头像按角色配色，主 Agent 蓝底"主"）/ 用户介入右侧绿气泡（#95EC69/暗色 #3eb575）+ CSS 三角尾巴 / 工具动作行居中灰系统消息 / >5 分钟才显示居中时间条 / 文件卡片 / typing 三点动画。**数据逻辑零改动**
- 新组件 `AgentAvatar.vue`（头像配色集中）+ `InterventionBar.vue`（微信输入条，嵌入作战室底部）
- JournalEntry kind 扩展 `'intervene'`；ChatReplay 继承新样式

### 独立移动端前端（与桌面完全解耦）

- **新工程 `mobile/`**：Vue3 + Vite + Vant 4 + vue-router（`createWebHistory('/m/')`），与 web 零代码共享、只共用后端 REST/WS
- **全界面微信风格（2026-09-05 追加，用户要求"完全按照微信风格"）**：
  - 全局微信色板与 Vant 主题变量全量覆盖：主绿 #07C160、危险红 #FA5151、警示橙 #FA9D3B、链接蓝 #10AEFF、页面底 #EDEDED、文字 #191919/#888/#B2B2B2；暗色对齐微信深色模式（#111/#191919）
  - 任务列表 = 微信**会话列表**心智：48px 圆角方头像（执行 Agent 角色色）+ 标题行/摘要行（"[执行中] 节点名 — agent"）+ 右上角会话时间（今天 HH:MM/昨天/M月D日）+ 红点与数字角标（待审批/需澄清）
  - Agent 总览 = 微信**通讯录**心智：分组标签（主 Agent/开发组/测试组…）+ 成员 cell（忙碌橙点、正在执行节点、模型徽标）
  - 任务详情 = 微信**聊天页**心智：导航标题"任务名(成员数)"、聊天区纯净 #EDEDED、Tab 改"聊天/执行详情/事件"；执行详情与事件改微信分组列表（圆角白卡 + hairline 分隔 + 右箭头）
  - 发起/澄清/计划确认 = 微信**设置页**心智：灰色分组标题 + inset 白卡 cell + 微信绿主按钮（.wx-btn/#07C160，按压 #06AD56）
  - 聊天流气泡绿 #95EC69（暗色 #3EB575）、发送条为微信输入栏样式；Tabbar 图标"任务=chat/成员=friends"
- 六屏：任务列表（搜索/下拉刷新/加载更多/审批）、任务详情（成员头像条+微信聊天流+底部介入输入条/执行详情/事件）、发起任务（目录级联 Picker）、澄清问答、计划确认、Agent 总览
- 移动端专用健壮性：WS 指数退避重连 + visibilitychange 回前台立即重连并补拉；`VITE_API_BASE` 钩子（后续 Capacitor 打 app 即绪）
- 后端：staticMiddleware 支持前缀挂载，`/m/` serve `mobile/dist`（`COTEAM_MOBILE_DIST` 可覆盖），同源零 CORS
- 访问：手机同 WiFi `http://<局域网IP>:8855/m/`；manifest 可添加到主屏幕；深色跟随系统
- **桌面 web 零响应式改动**（按用户要求两端完全解耦）

### 移动端项目功能（2026-09-05 追加，用户反馈"下发任务不能选项目、项目进度监控不到"）

- **Tabbar 新增「项目」页**：项目列表（微信 cell 心智：项目图标+进行中红点+任务/完成/问题统计+完成度进度条）
- **项目详情页**（进度监控）：统计头（任务/完成/执行中/失败四格+总进度条+「⚠ N 个任务需要你处理」提醒）+ 项目内任务列表（会话行心智，状态标签：执行中/待处理），点任务直达作战室；右上角 + 下发任务自动带入项目
- **发起任务支持选项目**：新增「所属项目」cell（不关联项目/项目选择器），选中后**自动带出项目 workspace**；从项目页进入时 query 预选
- 任务列表页项目任务显示项目名 tag
- **后端修复（Bug-5）**：`listProjects` 用 `project:*` 模糊匹配把 `project:xxx:memory`（项目记忆数组）也当项目返回，且无 shape 校验——历史污染数据直接顶掉项目列表（表现为接口返回无 id/name 的怪对象）。修复：过滤 `:memory`/`:deleted` 键 + 校验 `p.id/p.name` 才收录；修复后列表干净返回真实项目
- 验证：项目页渲染 2 项目、co-team 详情统计头+6 任务、项目下发预填（co-team + D:\pxx\co-team）、默认"不关联项目"；projectMode 测试回归通过

### Agent 技能系统 + 执行骨架 Harness（2026-09-05）

- **Harness 执行骨架**（`server/src/harness.ts`）：七层结构化 system prompt（身份/不可协商准则/协同上下文/工作流契约/工具策略+轮次预算/输出契约/升级语义）替换平面拼接；`validateAgentResult` schema 门控（summary/verification 必填、字段类型校验），违规走精准修复轮；AgentResult 新增 `verification` 字段
- **Skill 技能系统**（`server/src/skills.ts`）：SKILL.md 约定（frontmatter+正文），全局库 `skills/` + Agent 专属 `agents/<x>/skills/`；agent.yaml `skills:` 显式绑定全量注入，未绑定按 tags/关键词自动匹配（上限2）；API 五端点（CRUD+reload）；设置面板「技能库」tab + Agent 绑定多选；种子技能 code-review-checklist
- **节点交付成果**（`server/src/deliverable.ts`）：统一模板（做了什么/目标贡献/变更/验证/问题），三终态（完成/接管成功/失败）自动生成，KV+journal（kind `deliverable`）卡片，作战室点击阅读（双端弹窗渲染）
- **项目进度成果表**：`GET /api/projects/:id/report`（每任务×每节点完整记录+交付成果内嵌），OfficeView「进度成果表」对话框（任务行展开节点明细），移动端项目页「成果表」页
- 真实任务验证：m3wht3qe「分析一下当前项目的缺陷」——技能自动装载日志、deliverable journal 卡片、统一模板报告全部落地
- 回归：152 用例全绿

### 为后续路线留门（未实施）


- iOS/安卓 app：移动端工程 + `VITE_API_BASE` 就绪，Capacitor 打壳即可（需 HTTPS 或资源打包）
- 飞书 bot：notify 出方向已有；收消息需回调端点 + 公网 + **鉴权前置**（当前 API 裸奔，任何对外暴露前必须先加最小 token 门禁——红线）

---



| 优先级 | 问题 | 状态 |
|--------|------|------|
| P0 | Orchestrator 未使用 LangGraph StateGraph | ✅ 已重构：StateGraph + 条件门控 + reducer，保留线程驱动兜底 |
| P0 | 无 Sandbox，Agent 可操作任意文件 | ✅ 已实现任务级沙箱 + 路径逃逸防护 + 命令白名单 |
| P1 | MemoryBus 不持久化 | ✅ JSON 落盘（exit flush + close flush），重启恢复 |
| P1 | LLM 规划 edges 经常为空 | ✅ 已实现 fallback：无效边自动生成线性链 |
| P2 | Dashboard 无历史任务 | ✅ 任务列表持久化 + 状态实时同步 |
| P2 | 无 token 统计 | ✅ usage_metadata 统计 + 成本核算 + 预算控制 |
| 新增 | WS 在事件循环中同步阻塞迭代 pubsub（打开页面后 API 全挂） | ✅ 已修复：后台线程读取 + asyncio 队列 |

---

## 里程碑时间线

```
Week 1-2:  ✅ 基础可用，能跑通 demo
Week 3-4:  ✅ 执行引擎，Agent 能操作文件
Week 5-6:  ✅ 专业 Agent，各司其职
Week 7-8:  ✅ 智能调度，并行执行
Week 9-10: ✅ 监控通知，可观测
Week 11-12: ⏳ 产品化，pip install（打包发布待完成）
```

---

## 竞品参考

| 项目 | 定位 | Co-Team 差异 |
|------|------|-------------|
| Devin | 全自主 AI 工程师 | Co-Team 更轻量，个人开发者友好 |
| OpenHands | 单 Agent 开发 | Co-Team 多 Agent 协作 |
| AutoGen | 多 Agent 框架 | Co-Team 开箱即用，非框架 |
| Cursor | AI 编辑器 | Co-Team 独立运行，跨 IDE |
| Hermes Agent | 编排引擎 | Co-Team 内置模型池 + 插件系统 |

---

## 核心设计原则

1. **可见性优先** — 用户随时知道每个 Agent 在做什么
2. **渐进式复杂** — 简单任务 1 个 Agent，复杂任务自动拆解
3. **失败可控** — 任何 Agent 失败都有明确的恢复路径
4. **零配置** — 内嵌依赖，开箱即用
5. **插件化** — Agent、模型、通知渠道均可扩展

---

## 验证记录（2026-09-02）

- 单元/集成测试：`tests/` 4 个文件全部通过（router / scheduler+sandbox+tools / orchestrator（重试、升级、并行、审批、取消、上下文共享、持久化）/ transport 持久化）
- LangGraph 驱动专项验证：并行 fan-out/join、取消、审批挂起→恢复、失败级联取消下游
- 端到端实测（真实 LLM）：提交「创建 hello.py + pytest 测试」任务 → 自动规划 dev→test 两节点 → 沙箱内生成文件并通过测试 → 合并回工作区 → git 自动创建分支 `coteam/task-*` 并提交
- 服务冒烟：`/api/status`、`/api/agents`、`/api/metrics`、`/api/tasks`、`/docs`、`/`（仪表盘）全部 200

---

## 前后端全 TS monorepo 重构（2026-09-03）✅

原单文件 HTML 仪表盘 + Python 后端已重构为全 TypeScript monorepo，目标 `npm i -g co-team` 零 Python 依赖安装（对齐 hermes/openclaw 分发体验）。

| 模块 | 状态 |
|------|------|
| server/（Node + Hono + ioredis + openai SDK + simple-git） | ✅ 逻辑 1:1 照搬已验证的 Python 设计：并行 DAG、重试→接管→人工介入、审批、取消、沙箱、git、对话记录、模型降级/成本选型 |
| web/（Vue3 + Vite + Element Plus + ECharts） | ✅ 1:1 平移全部仪表盘功能（任务表单+目录选择器、agent 卡片、任务列表、事件日志、模型池、指标、对话回放、审批/取消） |
| 新功能：任务 DAG 拓扑图（ECharts 分层布局） | ✅ |
| 新功能：Agent 负载图 + 模型 Token 图表 | ✅ |
| vitest 测试（复刻 Python 套件，23 用例） | ✅ 全绿 |
| 真实 LLM 端到端 | ✅ 规划→沙箱→落盘→git 自动提交→对话记录/DAG 可见 |
| API 契约 | ✅ 与 Python 版逐字一致，调用方零迁移 |
| Python 原版 | 📦 移入 `legacy-python/` 存档 |
| npm 发布 | ⏳ bin/files 已配置，本地 `node server/dist/index.js` 可跑；待正式 publish |

常用命令：`npm run build`（构建全部）、`npm run dev:server` / `npm run dev:web`（开发）、`npm test`、`npm start`（启动服务，端口 8855 不变）。

---

## 十项改进落地（2026-09-04）✅

按《Co_team项目改进Agent执行任务文档.md》完成 10 项关键改进（安全沙箱强隔离按文档要求搁置，仅在 README 标注实验特性；Git 管理已补齐）。

| # | 改进项 | 实现 |
|---|--------|------|
| 1 | 项目初始化工作流 | `POST /api/projects` 支持 `scaffold: true`：docs/src/tests/config 目录 + README/CONTRIBUTING/ARCHITECTURE.md 模板生成 + git init/根提交；UI 新建项目可选 |
| 2 | Git 管理 | 项目创建自动 git init + 提交规范落 CONTRIBUTING 模板（分支工作流此前已完备） |
| 3 | 知识与经验沉淀 | 服务端统一目录（data/knowledge/{general-tech,projects/{id}}）Markdown+frontmatter 存储；标题去重更新；关键词检索；agent 经 `write_knowledge` 工具自主沉淀；任务结束复盘自动写入；planner/agent 提示词注入相关条目；UI 知识库管理面板（增删改查/搜索/编辑器） |
| 4 | 文档驱动协同 | SSOT 文档协议：TASK_SPEC/API_CONTRACT/STATUS_REPORT 版本号+哈希注册表，`doc_updated` 事件广播，执行前文档检查（缺失/篡改自动恢复），原子写入；**行为契约已补齐**：agent 经 `write_doc` 工具自主更新协同文档（版本注册表+作战室卡片，成果归属写入 upstreamContext），经 `send_message` 相互留言（延迟派发：收件方下次 dispatch 注入，orchestrator/user 留言在节点执行前落作战室，任务终态未送达消息标「未送达」归档）；docsSection 短文档全文注入提示词 |
| 5 | 需求理解与澄清 | LLM 需求清晰度评估（目标/约束/验收），不达标进入澄清循环（最多 3 轮），人类「确认」后才规划执行；UI 澄清对话框 |
| 6 | 双向进度沟通 | 统一进度消息（percent/ETA/当前节点）；`GET /api/tasks/{id}/progress` 查询；主动汇报 30s 节流防轰炸 |
| 7 | 任务分级处理 | light/standard/heavy 三级（规则+人工覆盖），分级映射计划模板（节点数/文档要求/评审节点）；UI 分级选择器 |
| 8 | 测试修复闭环 | 测试输出解析器（pytest/vitest/jest）；失败测试阻止提交，解析错误→定向修复→重测（轮次可配，默认 3）；每轮入作战室日志；超限上报人工 |
| 9 | 异构技能协同 | GLOBAL_GOAL 全局目标：执行时生成、注入每次 agent 调用、节点 goal_link 贡献声明；目标更新广播并失效 agent 会话；UI 查看/编辑 |
| 10 | 快照与回滚 | git 引用 + 协作状态 KV 导出的显式快照（task-start/decision/task-end/手动）；索引按任务/标签查询；回滚需确认+审计日志；沙箱硬重置/工作区建回滚分支；UI 快照列表+一键回滚 |
| 11 | 主 Agent 模型锁定 | 创建时手动指定（含模型池通断展示）即锁定，replan 沿用；故障允许降级但必留痕；中途可手动更换（API+UI，作战室留痕） |

新增模块：scaffold/templates、knowledge、clarify、grader、ssot、testloop、snapshot、progress；配置新增 `knowledge.dir`、`orchestrator.max_fix_rounds`。
测试：vitest 15 文件 76 用例全绿（新增 scaffold/knowledge/progress/ssot/snapshot/clarifyFlow/testFixLoop/clarifyGraderTestloop 8 个文件）。

---

## 群组沟通 → 方案转项目 → 经验沉淀（2026-09-08）✅

新增一条完整产品链路：**用户发起群组讨论（勾选 agent）→ 自主沟通打磨项目规划方案 → 一键转项目+开发任务 → 讨论期/开发期经验自动沉淀到项目并可查看**。

| 能力 | 实现 |
|------|------|
| 群组讨论引擎 | `server/src/discussion.ts`：讨论/消息全走 message bus KV（Redis/内存双支持）；每轮全员被唤起、各自输出 `{speak, reply, experience, ask_user}` 自决发言或沉默（无关 agent 不强制）；@ 点名成员必须发言（speak:false 时定向追加重试一次）；LLM 调用失败/输出不可解析一律诚实沉默，绝不编造发言；轮末系统消息登记发言/沉默/提问名单 |
| 双模式节奏 | 手动（每条用户消息触发一轮 + 「继续讨论」自由轮）/ 自动（主持判定收敛，单次上限 3 轮，可随时停止）；详情页随时切换；并发锁 busy（10 分钟 TTL）+ 启动清扫 clearStaleDiscussionLocks（仿 E7 僵尸任务清扫） |
| 用户=决策方 | 用户发言标注「决策方」注入每个成员上下文，补充与方向性修正有约束力；agent 遇方向取舍/资源决策/重大分歧用 `ask_user` 提问（@你 待拍板高亮，回复后派生「已回复」）；自动循环检测到待回答立即暂停；桌面通知 + 导航徽标 |
| 方案生成 | 主持人 LLM 收敛讨论为结构化《项目规划方案》markdown（背景/范围/技术方案/里程碑/风险/经验决策摘要/待定事项=未回答提问）；可重新生成、可人工编辑（版本 +1） |
| 方案转项目 | 新建项目（scaffold/git init）或挂入已有项目；讨论即澄清（任务 skipClarification + planAsync）；方案全文+来源标注+待定事项作为任务描述；方案沉淀项目知识库（category=project，source=discussion:{id}）+ 项目记忆；auto_run 时轮询 planned 后进队列 |
| 经验沉淀 | 成员发言携带 experience → writeKnowledge（挂项目则项目归类，tags=[群组讨论, agent]，source 标注）+ agent 个人记忆；开发期沿用既有任务复盘自动沉淀；GET /api/knowledge 新增 source 前缀过滤 |
| API | `/api/discussions` CRUD + messages/round/stop/scheme/convert；新事件 discussion_* 走 emitProgress（/ws/events 零改动广播） |
| Web | 第 4 个导航 tab「群组沟通」（待拍板徽标）：GroupDiscussionView/DiscussionChat（微信气泡+ask_user 高亮+@chip 点名+打字指示器）/SchemePanel/ConvertDialog；OfficeView 新增「项目经验·知识库」区块 |
| Mobile | tabbar 新增「沟通」（徽标）：DiscussionListView/DiscussionDetailView（同款气泡+@chip+方案/转项目/经验弹层，marked 渲染）；ProjectDetailView 新增项目经验区块——双端同步达成 |

测试：`server/test/discussion.test.ts` 22 用例（自决沉默名单/@强制/ask_user 落库+自动暂停/待回答派生/诚实降级/busy 409/方案生成含待定事项/convert 建项目+建任务+沉淀/auto_run 进队列）；全量 40 文件 250 用例绿；三端 build 通过。
GUI 实拍（8856/8857，gui-test-screenshots/discussion-*）：真实 LLM 全链路——发起→3 轮自主讨论（test 自决补充、dev 被 @ 强制发言并 ask_user、用户拍板续聊）→方案 v1→转项目（脚手架+知识库条目+任务 17 节点 planned）→任务详情联动；测试产物已清理。

---

## 任务 git 落点治理 + 项目目录操作边界（2026-09-08）✅

用户反馈：下发的开发任务 git 全部落在 co-team 仓库里。取证：34 任务中 26 个 workspace 在 co-team 树内（17 自指属设计内 + 9 嵌套子目录属真 bug——checkIsRepo 向上穿透）；gitCommit 切任务分支不回切 HEAD 吸附后续人工提交；命令白名单只查首词、绝对路径参数畅通。

| 能力 | 实现 |
|------|------|
| 项目独立目录 | config 新增 `projects.root`（缺省仓库同级 projects 目录）+ `projects.selfdev_root`；`GET /api/projects/root`；新建项目缺省 `<root>/<slug>` 并自动独立 git init |
| workspace 校验 | `assertStandaloneWorkspace`：嵌套在其他仓库工作树内（rev-parse toplevel≠自身）或位于 co-team 内 → 400（自指需显式 allow_self_ref）；接入 projects/tasks/discussion-convert 全部入口；审批/澄清重入队的 `'.'` 兜底改校验（空即 400，不再落服务 CWD） |
| 目录监狱 | `assertWithinJail`：命令中的越界绝对路径/`..` 一律拒绝（executeCommand/Async + applyFinalOutput 审批预检，full 也不能越狱）；harness L5 补目录边界纪律 |
| HEAD 回切 | gitCommit 记录并回切原分支；启动清扫 `restoreStaleTaskHeads`（无独有提交才回切，有则保留告警）——实测正确 hold 住 calc-demo(2)/hr-system-v2(3) |
| 自指隔离 | 自指任务执行重定向到 selfdev_root 本地克隆（graph.selfdev_path），主副本零触碰；`DELETE /api/tasks/:id/selfdev` 清理 |
| 双端 UI | web/mobile 新建项目预填 root+slug、独立仓库提示、自指开关；TaskForm/TaskCreateView 自指开关；转项目弹窗预填；full 文案改「项目目录内完全控制」 |
| 存量清理 | `scripts/cleanup-workspace-legacy.mjs`：已删 5 条空任务分支 + 已 squash 的 gnq6h5p6 + tmp-* 目录 + 嵌套项目记录；m3wht3qe/xzimxize（2 条 docs 独有提交）待用户定夺 |

测试：新增 jail 7 + workspaceGuard 7 用例，全量 43 文件 273 用例绿；三端构建通过；API+GUI 实测：自指任务 400 带指引、无 workspace 建项目自动落 `D:\pxx\projects\<slug>` 独立仓库、双端新建弹窗预填与提示、启动清扫日志正确。

## LLM 超时语义重做 + 缓存优先上下文裁剪（2026-09-09）✅

用户反馈：accountapp 任务 i6efv5h2 夜间执行失败。取证：三个模型死于三种不同原因，但日志一律记成 `all models failed (1 tried): LLM 调用在 600000ms 超时后被中止（上游流停滞）`——归因被超时机制本身污染。

根因链：
1. **时长被当成失败**：`AbortSignal.timeout(600000)` 是总时长上限，只要模型没在 600s 内生成完就杀，与"是否还在推进"无关。本地 31B/35B 模型面对 4 万 token prompt + 数万 token 思考，正常生成也必超 600s → Qwen3.6 / gemma 各被误杀 5 次。
2. **系统缺陷伪装成模型故障**：`grepFiles` 的 path 参数被模型传成文件（不是目录）时直接 `readdirSync(文件)` 抛 ENOTDIR，异常穿透 `applyToolCalls → callAgent → dispatch`，把 glm 的 5 次**健康响应**一并炸死，还计入模型健康度。
3. **上下文膨胀自伤**：max_tokens 无脑取模型上限（128000）、8 个技能全文进固定前缀、全局目标全文注入、flat 200 文件清单、工具结果逐轮全量重发 → 节点 6 单轮 44512 token / 日志 1.45MB。
4. 车道被 failed 任务阻塞 + 项目仓库无主干（唯一分支是 `coteam/task-*`），26 文件半成品孤悬。

改造（原则：**时长本身永不判死**；慢是降权问题，不是失败）：

| 层 | 改造 |
|---|---|
| 传输层 | 三类确定性判定：①连接/HTTP 错误、流无 finish_reason 即结束 = connection_died；②首 token 前 / token 间**静默**超线 = stream_stalled（任意 chunk 刷新计时，慢但活着永不杀）；③墙钟保险丝 `wallclock_cap_sec` 默认 0 关闭。`llm.ts` 新增 watchdog + `configureLlmTimeouts`，SDK 侧 `timeout: 86400000`（实测 0=立即超时而非禁用，故用大数等效禁用）|
| 节点层 | `agent.yaml timeout` 语义改为节点级总时长预算（缺省 300→3600s），轮间检查，超线 `[node-budget]` 停靠人工、不记模型失败；设置界面文案与提示同步 |
| 健康度 | `markSlow`（slow_success_sec=300）只降权（effectivePriority 累加 slowCount），不冷却；stall/died 才 markFailure |
| 分型分流 | `[env-defect]`（ENOTDIR 等 fs 异常）/ `[precondition]`（缺源码类申报）命中即终止模型阶梯转人工，不烧降级链与重试；终局错误输出「N 次尝试 / 链 m1→m2 / 各模型错误明细」不再谎报 1 tried |
| 上下文 | max_tokens 按复杂度分档 8k/32k/64k（初版 16k/32k 实测对推理模型过小——xfzrhwkc 中 Qwen3.6 思考满 16k 正文空，E5 正确判定但白烧一轮，故放宽）；技能改**索引 + load_skill 工具**（正文以工具结果追加）；目标/文件清单截断指向全文；`renderWorkspaceTree` 确定性目录树；`estimateTokens` 分段尺寸入 `prompt_profile` 与每轮 `telemetry`（含服务端 `cached_tokens`）|
| 缓存保护 | 工具循环严格 **append-only**；仅估算超 `context.max_prompt_tokens`(16k) 时对更早轮次做**一次性确定性折叠**（不经 LLM、同输入字节一致，重试可复用前缀），每尝试至多一次；重复工具调用指针化去重 |
| 工具 | `grepFiles` 文件路径改单文件匹配、异常软错误回喂；`readFile` 大文件截断注入而非报错 |
| 讨论链路 | 群组讨论发言轮改用自带墙钟上限 300s（不再复用 agent.yaml timeout，因其语义已变）——**同日已被讨论引擎 v2 再次取代**：发言轮墙钟关闭（0），只有路由/主持类幕后调用带 60s/90s 短保险丝 |

端到端实证（续作任务 xfzrhwkc，2026-09-09 上午）：节点 1「dataService 月度聚合」由 gemma-4-31b-it **单尝试 1824 秒（30.4 分钟）完整跑通**——同任务同节点昨晚在 600s 线下连败 10 次；Qwen3.6 烧满 16k 思考被 E5 正确判定快速降级（不再挂死 600s）；`Model slow but usable slow_streak:1` 降权记账生效；全程无 ENOTDIR；产物 45 行纯整数聚合实现经抽查零浮点。任务余下节点在车道内继续执行。

遗留新缺陷（xfzrhwkc 回归中实测发现，待排期）：
- **E16 dev watch 实例误杀在线任务**：用户 `npm run dev:server`（tsx watch src/）与正式实例共存时，watch 每次源码热重载都会在共享 bus 上跑一遍启动清扫（E7），把另一个实例**正在执行**的任务标 failed——执行器无感知继续跑，任务状态与实际产物从此脱节（xfzrhwkc 即受害：08:00 被标 failed 后节点 1 仍在正常出轮）。改进方向：sweep 前校验任务属主（执行器 pid 心跳键），只清真正无主僵尸；或实例启动时抢占编排锁，非主实例跳过 sweep。
- **E17 推理模型输出预算与分档的适配**：16000 档对 Qwen3.6 不足（思考满额正文空、白烧 174s/轮后 failover），已放宽默认到 8k/32k/64k；后续可让 tier 跟随节点类型（含大文件产出的 dev 节点上浮），或在 prompt 里约束思考规模。

缓存方向验证：现场读 vLLM 计数器 `prefix_cache_hits/queries = 133267872/143205167 ≈ 93%`，证明"静态前缀 + append-only"确实在被服务端复用——折叠每尝试仅一次正是为了守住这个命中率。
测试：新增 llmTimeout 9 用例（慢流不被杀/首 token 与 token 间静默判定/chunk 刷新计时/墙钟保险丝/connection_died/cached_tokens 透出/缺省值）+ contextBudget 14 用例（token 估算、目录树确定性、ENOTDIR 修复、折叠确定性与低频、分档封顶、三类分流、健康度不污染）+ skills 索引化/load_skill 断言改造；dailyReport 分型正则收编新文案；全量 45 文件 295 用例绿；server/web/mobile 三端构建通过。

## 群组讨论引擎 v2：像真群聊 + 组内长手（2026-09-09）✅

**起因（用户实拍体验反馈）**：群组讨论"像回合制游戏，解决不了实际问题，启动个项目都启动不了"。Redis 历史取证（0l7ybpte「运行项目」52 分钟 8 轮全空转）定位根因：
- 回合制：`runRoundCore` 按成员数组固定顺序全员串行点名（沉默也要付一次 LLM 往返）；轮内用户消息被 409 拒收；@点名不禁止他人发言；发言整条蹦出无流式；每轮盖"第 N 轮 · 发言：…"印章；发言失败静默降级成"未发言"（launcher 被 300s 墙钟砍死用户不可见）。
- 没有手：发言是纯文本 JSON 契约（chat() 无工具通道），提示词却要求"给可执行建议"——模型只能虚构：launcher 6 次宣称"已下发启动指令"（node_modules 不存在、5123 无监听、日志目录不存在）、deploy 宣称"已写入 vite.config.js"（文件 mtime 早于讨论 12h）、front-dev 编造不存在的项目根目录。
- 附带缺陷：Redis 无持久化（一次重启 nircnkrg 讨论整体蒸发、前端对 404 轮询 100 次）；同一句经验重复入库 4 次；上下文仅 30 条失忆。

**改造（server/src/discussion.ts 整文件重写为引擎 v2）**：
| 块 | 内容 |
|---|---|
| 路由选人 | 每轮先由 simple 档模型按"用户最新指示+现状"选 1-3 位真正有新东西的成员；@点名=只跑被点名者（禁言由构造满足）；路由失败回退全员轮转。**路由/主持类幕后调用带 60s/90s 墙钟**——推理模型可能长时间只吐 reasoning_content 不出正文（glm-5.3-flash 实测），按"任意 chunk 活着"永不断流，幕后调用宁可判错不可让用户等（E18 教训：p65y6inq 首轮挂 26 分钟）|
| 轮内真打断 | 用户消息随时入库（409 废除）；每位发言者之间检查新用户消息，有则终止本轮剩余成员并重新路由；busy 锁只防循环并发；锁释放后 drain 复查防消息掉缝 |
| 组内工具面 | 发言契约扩展 `tool_calls`（形态 A 动手/B 发言），单轮最多 5 迭代、末次强制收尾；只读侦查复用 `applyToolCalls`；`exec`（同步 ≤90s）/`exec_background`（长驻+日志落项目 .coteam-logs）/`kill_process` 过 `assertWithinJail`+`config.permissions` 同等闸门；**小改代码** `write_file/edit_file`（单轮 ≤3 文件 ≤80 行，写前自动 git checkpoint `coteam: discussion <id> pre-write checkpoint`，可回滚）；超预算软错误引导转任务。工具过程落进讨论流（🔧 活动行）+ `discussion_tool` 事件——过程可见即幻觉可审计 |
| 全量背景注入 | 绑定项目的讨论把**真实 workspace 绝对路径、package.json scripts、git 近 5 提交、项目记忆全量（去 10 条限）、project 知识库全量（去 top-3 限）**作为所有成员共享的静态前缀（system 消息逐字一致保前缀缓存）；超 24k 字符按最旧经验省略并显式标注 |
| 真实性纪律 | system prompt 最高优先条款：宣称已完成必须有本轮工具结果支撑；路径/配置以注入与工具结果为准；做不到直说转任务 |
| 流式与反馈 | llm.chat 第 7 参 onDelta → 增量提取 reply 字段发 `discussion_message_delta`（工具轮 discarded 清除）；去轮次印章；发言失败可见 notice；emoji 回应（react）与引用回复（reply_to）；经验沉淀按讨论内包含去重 |
| 启动恢复 | `resumeOrphanedDiscussions`：重启打断在飞轮导致"最后一条是用户消息"的悬空讨论自动重驱动（实测：0l7ybpte 悬空 30 分钟的追问被恢复轮接住，launcher 发现 5123 已死 → 重新后台拉起 → netstat 验证 → 服务复活）|
| UI 双端重做 | 微信式群聊质感：同发送者分组（仅首条头像+角色名，中文角色着色）、我侧无头像无名字、悬停时间戳、工具灰条、系统卡片/轻灰 notice、未读胶囊+分隔线、流式打字气泡（"正在输入/正在动手执行/正在看执行结果"）、路由器"正在决定谁来回复"；mobile 导航头像堆叠+右上 popover 收纳操作、底部常驻输入+@面板+长按动作（复制/引用/👍✅👀）；web/mobile 双端一致 |
| 持久化 | Redis 开 AOF（CONFIG SET appendonly yes + REWRITE + 全量 BGREWRITEAOF 31MB 落盘，重启后历史讨论实证存活）；前端 GET 404 自清 |

**端到端实证（还原用户抱怨场景）**：新建讨论绑定 accountapp → "@launcher 用 5123 跑起来"：mention-only 轮 → `🔧 后台启动 npm run dev:h5 pid` → `🔧 netstat exit 0` → 汇报与实况一致（netstat 独立复核 LISTENING、curl HTTP 200 `<title>每月记账</title>`）；front-dev 群内 `edit_file vite.config.js`（checkpoint d2c4b62 在 accountapp git log 可查）；用户追问"能用 IP 访问吗"被注入的真实配置读取答对（host 0.0.0.0 + 防火墙提示）。
测试：discussion.test.ts 重写为 36 用例（路由选人/点名禁言/轮内打断重路由/工具循环真实执行/小改预算超限/路径监狱/全量注入+静态前缀逐字一致/delta 提取器/失败可见/去重/reactions/回复引用）；全量 44 文件 309 用例绿；三端构建通过；双端实拍验收。
配置：config.yaml 新增 `discussion:` 节（permissions/max_rounds/project_context_char_cap，见 config.example.yaml）；llm.chat 增 onDelta；GET /api/discussions/:id 回传 busy。
遗留：E18（幕后推理模型只吐 reasoning 的挂死风险已用短墙钟兜住，但发言轮同样可能长时间"正在输入"——后续可给讨论 speaker 配非推理模型档位）；exec_background 起的 dev server 在 zcode 后台任务清理时会被 job 连带杀掉（真实终端启动不受影响，未复现于生产路径）。

### 引擎 v2 追加（同日实测驱动）：转任务工具 + 承诺跟进 + lost-update 修复

p65y6inq 实测暴露三个缺口，均已落地：
- **convert_to_project 工具**：讨论里 agent 只能"建议转任务"、按钮在人手上——用户拍板"是的"后 front-dev 口头宣布"正式启动 UI 开发任务，分四步"却 0 工具调用掉球（它根本没有转任务的手）。现在 agent 可自己完成 方案收敛→转任务→入队（讨论即封存），实测转出了真实任务并入队。
- **承诺式收尾跟进**：发言含"正式启动/接下来我将/分四步…"等承诺且本轮未调用任何工具 → 响应循环自动追加一轮"追问"（只点名承诺者：要么用工具兑现、要么如实说明做不到并给人工路径），杜绝口头开工。
- **轮末 stale 覆盖修复（lost update）**：runRoundCore 轮末用轮初读的对象整键 saveDiscussion，把轮中 convert 写入的 converted/task_id/scheme 打回 discussing（MemoryBus 共享引用使单测无法复现，Redis 序列化才暴露）——轮末改为重读最新状态再 touch。
- **E19 遗留（待排期）**：planAsync 任务的后台规划与 auto_run 轮询是进程内浮动 promise，服务重启即成 pending 孤儿（0 节点、无队列记录、无恢复扫描）——与讨论 resume 同根因；方向：启动时把 pending+0 节点的任务重新驱动 assessThenPlan，或规划落盘断点续跑。本次实测中 phmwyt94 即死于重启窗口，已删除重转为 jr3gdkxq。
- 讨论引擎新增回归：convert 工具用例 + 承诺跟进用例（38 用例）；全量 44 文件 311 绿。

### jr3gdkxq 全程盯守实录（2026-09-09 晚）：E17 解法落地 + 交付验收教训

**E17 正式解法**：model_pool 条目新增 `chat_template_kwargs` 透传（llm.ts 流式/非流式双路径），Qwen3.6 配置 `enable_thinking: false`（vLLM 实测直出 JSON、reasoning=null）。效果：此前每节点先烧 6 分钟思考耗尽预算再降级（节点 2 卡死 40 分钟），修复后单轮 42s~6min 正常出正文，17 节点任务从寸步难行跑到 16/17。

**merge 节点失败处置**：节点分支互撞（coteam/1-review × coteam/2-front-dev）致 merge-auto 失败、任务 failed，但 partial recovery 已把 16 节点产物收进 task 分支——人工 `git merge coteam/task-jr3gdkxq` 入 main 完成 merge 语义，任务图状态修正为 success（节点 verification 记录实测证据）。

**交付验收教训（重要）**：任务"验证节点全绿"≠可用——合入 main 后浏览器实测发现页面全崩：dataService 缺 default export（四页统一默认导入直接 ReferenceError）、幻觉 fs/path 导入、createdAt 聚合口径串月、Object.keys 数字键匹配失败、wiring 脚本自身 seed 键名/清除顺序/eval export 多处错误。全部人工修复并 npm test 转绿、四页面真实数据流验收通过（录入 25.50→落盘 2550 分→统计/历史/设置全对）。**教训：launcher 验证节点在页面加载失败的情况下判了通过**——后续给验证节点的要求应包含"浏览器实际打开渲染确认"，而不只是脚本断言；已作为 deliverable 改进项记录。

### 验证纪律强化（jr3gdkxq 质检后续，2026-09-10）

用户复检发现"完成度很低"：development-habits 技能**实际被 load_skill 加载了 4 次**（journal 可证），但没有任何机制保证"加载=遵循"；验证节点（launcher/review）在页面加载即崩（dataService 缺 default export）的情况下判了全绿——其 verification 是代码走查而非运行证据。落地修复：
- **harness L4 验证条款重写**：验收点清单优先——任务描述/全局目标中每个显式要求逐条列清单，每条必须有真实执行证据（命令+输出/测试/实际打开页面），只读代码不构成验证，验证类节点尤其禁止走查代替运行；拿不出证据的条目必须 failed 或入 defects。
- **harness L6 高频错误新增**："代码看起来正确/应该能跑" 代替运行证据禁止（点名 default/具名导入不匹配、幻觉 import 只有实际运行才暴露）。
- accountapp 侧人工补齐（v1.1.0）：getMonthlyStats/getMonthList/getRecentRecords/exportBackup/clearAllWithBackup、设置页导出+清空二次确认闭环、统计页 CSS 环形图（canvas 饼图不符合任务要求）、文档三件套同步（STATUS_REPORT v19 修正 v18 虚高、CHANGELOG 建档）。npm test 全绿 + 浏览器实测导出 toast/两轮确认/备份键/分类保留全部通过。
- 结构性认知（不修则下次还犯）：技能是"索引+按需加载"，正文进上下文后无遵循度闸门；grader 只查 JSON schema 不查"验收点↔证据"对应。后续方向：verification 字段要求附命令输出摘要，deliverable 审计比对任务描述关键词与 changes/测试文件。

### 验证能力补刀：check_page 渲染工具 + 合并后全量验收闸（2026-09-10）

jr3gdkxq 归因结论的落地（"验证节点全绿但页面全崩"的三层缺口补两层）：
- **check_page 工具（tools.ts）**：系统 Chrome/Edge headless `--dump-dom --virtual-time-budget` 渲染 localhost 页面，返回 title/可见文本/expect 命中判定——零新依赖。动机实证：SPA 的 JS 运行时崩溃时 curl 返回 200 + 正常 `<title>`，命令级验证物理上看不见渲染缺陷。安全边界：仅允许 http(s)://localhost 系（防外联/SSRF）；Chrome 132 移除旧 headless → --headless=new 失败自动回退。接入任务管线 applyToolCalls、群组讨论工具面（未绑定项目也可用）、harness L5 清单；L4 同步规定"前端/HTTP 服务交付验证必须含 check_page 渲染确认，HTTP 200 与 title 不算"。
- **合并后全量验收（orchestrator/acceptance.ts）**：产物同步回真实工作区后、任务判 success 前，探测项目测试入口（package.json scripts.test / pytest 标记）并**实跑**；失败 → 任务 failed（error 带命令+退出码+输出尾部证据），无测试命令 → 不判死但 warn 留痕。分支沙箱与直写工作区两条路径都过闸。这补上了分支工作流的结构性盲区：每个节点只验证自己的切片，跨节点接口断裂（页面默认导入 vs 服务具名导出）只有在合并态整体运行才暴露。
- 测试：acceptance.test.ts 10 用例（detectTestCommand 五种标记、npm test 真实通过/失败、check_page URL 白名单拒绝 + **本机浏览器真实渲染断言**）；全量 45 文件 321 绿；三端构建通过。
- 仍缺的一块（诚实记录）：技能遵循度无硬闸门（load_skill 拉正文后模型是否照做无人检查）——方向是 verification 字段强制附命令输出摘要 + deliverable 审计比对任务描述关键词与产出，未动。

## 模型池服务分组编辑（2026-09-10）✅

**起因（用户反馈）**："现在的模型配置界面太死板了，同一个 baseUrl、同一个 apikey 下面应该可以配置多个不同的模型。"旧界面每个模型一张全尺寸卡片，Base URL/API Key 逐条重复填写，多模型服务配置冗余。

**设计取舍**：存储与调度契约不动——`model_pool` 保持扁平数组（`name` 是 ModelPool 唯一键，scheduler/llm/节点模型钉选全依赖它），分组只是编辑视图：设置界面按 `base_url + api_key` 自动归组为「服务接入点」卡片，公共字段在组头编辑一次同步组内全部模型，保存时展平回扁平数组。零迁移、运行时零触碰。

**改动**：
- server/llm.ts：`listUpstreamModels()`——OpenAI 兼容 `GET {base_url}/models`，20s 超时，返回排序去重模型 id。
- server/api：`POST /api/config/upstream-models`（参数缺失 400；上游失败 400 ok:false 不炸 500）；`PUT /api/config/model-pool` 增加全池重名拒绝（name 是唯一键，重名条目会互相遮蔽）。
- web/SettingsDialog 模型池 tab：服务卡片（组头 Base URL/API Key + 模型数 + 删除服务含级联确认）；组内模型紧凑行（名称/标签/六项调度参数/单独测试按钮）；「拉取模型」→ 勾选弹窗批量导入（自动排除已在池的）；「+ 添加服务」支持纯新端点。Agent 模型覆盖下拉改读展平后的全池。
- mobile：无模型池设置界面，接口契约未变，零改动（已核查）。

**验证**：upstreamModels.test.ts 6 用例（stub 上游排序去重/鉴权头/401 传播/端点参数校验/重名拒绝）；全量 46 文件 327 绿；三端构建通过；8855 实拍——tokenrhythm 与 192.168.1.237 两服务正确归组，拉取真实上游返回 22 模型且已在池的 glm-5.3-flash 正确排除，勾选联动计数正常，全程未改动线上配置（config.yaml 仍 2 模型）。

### 新模型重放实验（2y3tuote，2026-09-10）：一次重跑揪出四个引擎缺陷

用户换模型池（新增 qwen3.8-flash/deepseek-v4-flash/sensenova，删 gemma）后原样重放 jr3gdkxq 任务，accountapp 重置基线。结果与收获：
- **规划质量立判**：新池 68s 出 15 节点计划（含"启动 H5 冒烟验证""全量回归"节点，旧池 8 分钟+缺验证节点）；证据链纪律生效（各节点 verification 具体到 grep 命中行/命令清单，节点 14 实跑 6 条命令）。
- **E20（新）**：POST /api/tasks 的 plan_async+auto_run 组合中 auto_run 被 early-return 吞掉永不入队。修：autoRun 随 createTask→assessThenPlan→planInBackground 传递，planned 落地后经 orchestrator.onTaskPlanned 钩子（index.ts 接 taskQueue.enqueue）入队。
- **E21（严重·假 success）**：节点 failed→下游 cancelled→重试只重置 failed——下游永远躺在 cancelled，而 runGraph 终局判定不看 cancelled，半截交付直接报 success（14/15 完成、merge cancelled 仍 success）。修：终局有 cancelled 即 failed；重跑重置改为"非 completed 一律 pending"（E22）。
- **折叠×去重死链**：断崖折叠把旧 tool_result 换成摘要后，seenToolCalls 的"结果从略"指针成了死链——模型读不到文件（qwen3.8 明确报"read_file 被 harness 去重未返回正文"，节点 10 三连败）。修：折叠真正发生时 clear seenToolCalls。
- **模型侧**：sensenova/deepseek 429 tpm 限流反复烧链（429 是容量信号，后续应考虑不记 markFailure/短冷却）；sensenova/glm 在 simple 档 8000 预算重演 E17 思考耗尽（新模型也需 enable_thinking:false 类参数或上浮 simple 预算）。
- **check_page 自发使用 0 次**：L4 条款让证据变实（命令实跑），但模型仍不主动渲染验证——下一刀在 deliverable 审计：前端任务无 check_page 记录则降级/告警，而不是只靠 prompt。
- 人工收尾（非引擎问题）：recovery 产物散在分支/工作树，手工 merge 基底+stash 合并+清陈旧 branch 字段后跑通；最终 task success、post-merge acceptance（npm test）实战通过、四页面 headless 渲染实测 OK（entry/settings 全命中；stats/history 空态正确）。

### 流畅度改造 M1-M6（2y3tuote 分析落地，2026-09-10）

按《任务执行流畅度分析-2y3tuote.md》的清单实施，47 文件 335 用例绿：
- **M1 合法 blocker 停阶梯**（orchestrator LEGIT_BLOCKER_RE，模块级导出）：模型申报"[blocker] 契约 v1/v2 不符 / 需要补充信息 / 证据未获得"是**对环境的正确发现**——命中即停阶梯、不记模型健康度、直接 needs_human（此前 node2 让 6 个模型把同一问题重新发现了一遍）。
- **M2 SSOT 传播修复**（git.commitAllOnBranch + finalizeNodeSuccess）：节点分支提交从"只提交模型申报的 changes"改为**工作树全量提交**——unreported 漏报是系统常态，node1 的契约 v2 就是这样丢的（下游 6 连 blocker 的物理根因）；branch 创建失败时产物直提 base 兜底，绝不留在工作树等 checkout 冲掉。
- **M3 429 容量退避**（CAPACITY_RE）：限流类错误同模型 20s/40s 退避重试（tpm 窗口分钟级自愈），不 markFailure 不烧链；两次退避仍 429 才跳过该模型（仍不记失败——容量≠无能）。
- **M4 失败反馈进链序**（scheduler.effectivePriority）：failCount 计入排序（每次失败降 2 级、30 分钟线性回血、成功清零）——冷却是硬闸这是软序，坏模型不再每个节点首撞；"思考耗尽 finish=length"从 content 通道补记 markFailure（此前走 result-failed 路径从不记账，sensenova 十次耗尽链序不动）。
- **M5 read_file 行范围**：`{"tool":"read_file","path":...,"line_start":N,"line_end":M}`，截断回显自带续读参数指引（grep 定位→按段读）；大文件节点不再 5 轮拼不出完整现场。
- **M6 重跑 branch 自愈**：executeTask 在 ensureBase 后校验 completed 节点的 branch 是否存在于当前沙箱，不存在（上次沙箱已销毁）自动清空——merge 不再假冲突（本次两人工清场的自动化）。
- 新增 resilience.test.ts 8 用例（blocker/容量正则的命中与不误伤双向、软沉底与回血、行范围续读、全量提交下游可见性）。

## 架构改造周：滚动规划 + 实时问答 + 监督者 + 验收体系（2026-09-12）✅

用户拍板的六项架构级改造全部落地（M1-M5+M7，各自独立提交可回滚；379 用例全绿、三端构建通过）：

- **M1 连续调度器**（c202212）：runGraph 废除 `Promise.all` 波次制，改常驻调度循环——节点就绪即发射，不等"整波最慢节点"；`maxWorkers` 从摆设变为真信号量（此前一路传到 executeNodeInner 后变成 `_workers` 从未被消费）；STATUS_REPORT 同步/进度广播/失败 cancelDownstream 从波后迁到节点落地回调，失败节点的下游取消提前一个波次。
- **M2 全员实时问答**（753ecba）：新增 `askGate.ts`（持久化+内存 resolver 双层）与 `ask_user/ask_agent/answer` 三件套——agent 可发起阻塞式提问挂起等回答（等待不占模型槽位）；目标 agent 执行中→轮间注入实时转交（复用插话注入点），空闲→图外咨询 dispatch（单次 chat 拿专业判断，仿 discussion runRoundCore，不建节点）；API `POST /asks/:askId/answer` + 双端 ask/answer 气泡（ask 高亮待答+内联答复框）；同名 agent 并发的收尾清扫按计数收敛（兄弟 dispatch 结束不误杀在途提问）；服务重启由 execute 入口 `settleTaskPendingAsks` 惰性落盘。
- **M3 监督者**（67ad5a5）：新增 `supervisor.ts` 常驻服务——订阅 TASK 事件流（node_error/重试≥2/ask 超时/心跳），每次触发一次 simple 档 LLM 评估；有边界自动处置=催办（走插话通道，回应义务门保证必答）/汇报/建议，全落 journal；改图类（重试失败节点/砍子树/插节点）只生成提案写 `task:proposals`，作战室双端卡片一键批准后执行；min_interval_sec 防自激。
- **M4 滚动规划**（e7e8f6f）：planner 新增 `generateStagePlan`——每次只规划"可运行最小骨架"级阶段子图 + **结构化验收清单**（{requirement, evidence_type: unit|build|e2e|command|manual, target_platform}）；阶段闸门三出口：过→replanner 携带实际产出（阶段历史/清单进度）规划下一阶段、缺陷→修复节点插回、完成声明被未达成清单拦截→转人工；节点 id 加 `s{N}-` 前缀、阶段根挂上一阶段 merge 节点之后；`planning_mode: rolling|static` 缺省 rolling，static 完整保留回退。
- **M5 验收体系**（fd6ea99）：acceptance.ts 升级为**平台探测**（package.json+manifest.json → web/h5/miniprogram/app/api/cli 交付端集合+测试手段矩阵）+ **清单机审**（build/unit/e2e 三类用真实命令出证据：`npm run build`/`npm test`/`npx playwright test`，每项只有"机器证据"或"显式降级留痕"两种终态）；最终验收闸：滚动任务 success 后机审清单，红灯/降级项→waiting_approval + **派生修复任务提案**（一键批准→自动 createTask 继承工作区/项目→planAsync+autoRun 全自动执行，双向可追溯）；双端验收报告卡片（端级矩阵+逐项证据）。
- **M7 上下文管理**（ea7b678）：角色差异化裁剪——review 注入完整 diff（新 `gitDiffFull`）、test 注入测试资产索引（新 `listTestAssets`）、知识注入按角色关键词加权；折叠线按模型窗口动态化 `min(窗口×0.6, 窗口−4096)`，complex 节点×1.5，未知窗口回退全局常量；会话环形窗口 20→30；compactAssistant 保留 files。**顺带挖出陈年隐患：`git_diff` 工具白名单字段误传 snake_case（`whitelist_commands` vs `whitelistCommands`）+ 未传 maxTimeSec（timeout=NaN），工具一直静默失效**。
- **预检提交 E25**（e6fdba0）：并行兄弟节点分支收敛 `mergeIntoCurrent`——下游切分支时把其余上游兄弟分支全部并入（g6704zpm 回归节点把已完成的四页判为占位符的根因）。

测试：新增 continuousScheduler/askGate/supervisor/rollingPlanner/acceptanceGate/contextMgmt 六个测试文件 47 用例（就绪即跑/限流/即时 cancel/审批门不阻塞其他分支/问答三场景端到端/提案往返/滚动三出口/平台探测/机审降级留痕/派生一键批准），全量 379 用例绿；三端构建通过。
遗留与取舍（诚实记录）：① ask 阻塞等待在进程内存，服务重启时挂起提问按"无回答"落盘（与 E19 同根因，未做跨重启状态机）；② 独立验收 agent 的对抗式审查 v1 以"机器审计不可被 LLM 覆盖"+人工裁决兜底实现，E2E 场景由验收 agent 自动编写尚未做（环境具备时 playwright runner 即生效）；③ 无人值守端到端回归（M6）待用户起真实模型池跑首个滚动任务验证。
