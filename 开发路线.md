# Co-Team 开发路线图

## 项目定位

面向个人开发者的多 Agent 协作开发助手。核心理念：用户提需求 → 自动拆解任务 → 分发给专业 Agent → 实时可见 → 结果汇总。

> **当前状态（2026-09-02）**：阶段一至阶段五的核心功能已全部实现并通过测试与端到端验证；
> 阶段六产品化中与打包相关的事项待发布时完成（详见各表状态）。

---

## 阶段一：基础可用 ✅ 已完成

**目标：单机跑通核心链路，能用起来。**

| 模块 | 状态 | 原待完成项 |
|------|------|--------|
| Orchestrator | ✅ LangChain 集成 | ✅ LLM 规划 prompt 调优（含 agent 职责/记忆注入）、edges 自动生成（无效边自动回退为线性链） |
| Model Pool | ✅ 基础调度 | ✅ 模型降级策略（fallback_chain + 健康度冷却）、超时/槽位等待 |
| Plugin System | ✅ 发现 + 加载 | ✅ 动态热加载（`POST /api/agents/reload`、`coteam` CLI）、版本管理（agent.yaml `version` 字段） |
| Router | ✅ 规则匹配 | ✅ LLM 智能路由（规则 → 语义 LLM → 关键词三级回退） |
| Transport | ✅ MemoryBus | ✅ Redis 持久化（可用时自动启用）、MemoryBus 落盘（`COTEAM_STATE_FILE`）、消息确认（低优先级，暂以 Redis 自身可靠性替代） |
| Dashboard | ✅ 中文界面 | ✅ Agent 卡片交互（真实团队列表 + 实时状态）、进度条动画 |
| CLI | ✅ 基础命令 | ✅ 多轮对话（`coteam chat`）、`coteam setup` 可写入 API Key |
| API | ✅ REST + WS | ✅ 执行端点（`POST /api/tasks/{id}/execute`）、任务取消（`POST /api/tasks/{id}/cancel`）、审批（`POST /api/tasks/{id}/approve/{node}`） |

**阶段一交付物：✅ 全部达成**
- ✅ 用户可通过 CLI 或 Dashboard 提交任务
- ✅ 任务自动拆解为 DAG 并执行（LangGraph 驱动，并行 fan-out/join）
- ✅ 实时看到每个 Agent 的工作状态（WS 推送，已修复事件循环阻塞 bug）
- ✅ 执行结果和文件变更可见（任务列表/事件日志/指标面板）

---

## 阶段二：执行引擎 ✅ 已完成

**目标：Agent 真正能操作文件，不只是返回 JSON。**

| 模块 | 优先级 | 状态 |
|------|--------|------|
| Sandbox | P0 | ✅ 任务级沙箱副本，成功后 merge 回真实工作区，失败即丢弃 |
| Git 集成 | P0 | ✅ 自动创建 `coteam/task-{id}` 分支并提交变更文件 |
| Tool System | P0 | ✅ 运行中可调 read_file/list_files（tool_calls 轮次）；最终输出 files 落盘 + commands 白名单执行（含超时、路径逃逸防护） |
| Token Budget | P1 | ✅ 池级用量统计 + 单任务预算（`orchestrator.token_budget`），超限快速失败并通知 |
| 失败重试 | P1 | ✅ N 次重试（RETRYING 状态）→ 主 Agent 接管（调整策略/温度）→ 人工介入标记（needs_human + 通知） |
| Agent Output 解析 | P1 | ✅ 从 LLM 输出提取 files/commands/changes 并真实应用，代码围栏/JSON 提取容错 |

**关键设计决策：✅ 已按此流程实现**
```
Agent 执行流程：
1. 接收任务描述 + 工作目录（含现有文件清单）
2. 调用 LLM 生成执行计划（可多轮请求读取工具）
3. 通过 Tool System 执行（读文件、写文件、运行命令）
4. 验证结果（commands 运行测试/lint，失败计入 errors）
5. 返回变更列表 + 摘要
```

---

## 阶段三：专业 Agent ✅ 已完成

**目标：每个角色有专业能力，不只是通用 dev。**

| Agent | 职责 | 状态 |
|-------|------|------|
| `dev` | 开发实现 | ✅ v1.1.0（新增 files/commands/tool_calls 输出协议） |
| `review` | 代码审查 | ✅ v1.0.0（blocker 问题自动判失败） |
| `test` | 测试编写 | ✅ v1.0.0（编写并运行 pytest） |
| `deploy` | 部署配置 | ✅ v1.0.0（handler 注入 requires_approval=true） |
| `docs` | 文档编写 | ✅ v1.0.0 |
| `refactor` | 重构优化 | ✅ v1.0.0 |

**每个 Agent 包含：✅ 结构齐备**
```
agents/<name>/
├── agent.yaml      # 配置：模型偏好、超时、标签、version
├── prompt.md       # 系统提示词（含输出 JSON 契约）
├── handler.py      # 预处理/后处理钩子
├── tools/          # 工具说明（共享工具系统对接）
└── examples/       # 示例输入输出
```

---

## 阶段四：智能调度 ✅ 已完成

**目标：根据任务特点自动选择最优执行策略。**

| 功能 | 状态 |
|------|------|
| LLM 路由 | ✅ 规则匹配 → LLM 语义路由 → 关键词匹配三级回退 |
| 并行执行 | ✅ LangGraph fan-out/join（无依赖节点同超步并发），线程驱动兜底 |
| 模型降级 | ✅ fallback_chain 按优先级降级 + 连续失败健康冷却（60s） |
| 成本优化 | ✅ 节点 complexity（simple/normal/complex）→ simple 选最便宜、complex 选最强 |
| 上下文共享 | ✅ 下游节点 prompt 注入上游节点 summary + 变更清单 |
| 记忆系统 | ✅ 跨任务经验（成功/失败教训）注入规划 prompt（保留最近 20 条） |

---

## 阶段五：监控与协作 ✅ 已完成（协作模式多人共享除外）

**目标：可观测性 + 通知。**

| 功能 | 状态 |
|------|------|
| 执行日志 | ✅ 事件总线推送全部生命周期事件（含 retry/escalate/approval/cancel），Dashboard 事件日志展示 |
| 性能指标 | ✅ `GET /api/metrics`：token 消耗、成本、成功率、Agent 负载；Dashboard 侧栏指标面板 |
| 通知推送 | ✅ `src/notify`：webhook（`COTEAM_WEBHOOK`）+ 飞书（`COTEAM_FEISHU_WEBHOOK`），任务完成/失败/需审批/需人工介入均触发 |
| 审批卡片 | ✅ requires_approval 节点暂停执行 → Dashboard 一键审批 → 断点续跑 |
| Dashboard 增强 | ✅ 任务历史（含描述/状态/取消/审批操作）、模型池余量条、运行指标 |
| 协作模式 | ⏳ 多人共享任务视图（单用户定位，暂缓） |

---

## 阶段六：产品化 ⏳ 进行中

**目标：pip install 即用，零配置。**

| 功能 | 状态 |
|------|------|
| 打包分发 | ⏳ pyproject.toml 就绪，`pip install -e .` 可用；待发布 PyPI |
| 交互式配置 | ✅ `coteam setup` 引导式写入 API Key |
| 内嵌 Redis | ⏳ MemoryBus 已可无 Redis 运行并落盘；内嵌 redis-server 二进制待定 |
| 插件市场 | ⏳ 未开始（插件目录结构已标准化，具备条件） |
| 多项目支持 | ✅ 任务级 workspace 隔离（每个任务独立工作目录 + 沙箱） |
| API 文档 | ✅ FastAPI 自动 OpenAPI（`/docs`） |

---

## 阶段六.5：补差 + 聊天微信化 + 独立移动端 ✅ 已完成（2026-09-05）

**来源：《改进补差与聊天微信化计划.md》（R1–R10 遗漏补差 + 微信化 + 移动端），全量 120 测试绿。**

### R1–R10 遗漏补差

| # | 项 | 落地 |
|---|---|---|
| R1 | 执行中消息式介入 | ✅ `POST /api/tasks/:id/intervene`（KV 队列 + journal `intervene` 留痕 + `user_intervened` 广播）；orchestrator 每轮 userMsg 前消费队列注入"用户介入指示"块；未运行任务 400 拒绝 |
| R2 | 技能标签硬校验 | ✅ planner 输出 `required_skills`；normalizePlan 校验 agent tags 全覆盖，不满足改选交集最大者，无交集保持+warn（10 测试） |
| R3 | 知识库语义检索 | ✅ 14 组同义词表（js↔javascript、重试↔retry 等）+ 中文 2-gram/字符 Jaccard 模糊匹配（"登陆"命中"登录"），API 不变（6 测试） |
| R4 | 关键词查询 | ✅ `GET /api/tasks?q=`（description/task_id 不敏感模糊，兼容 project_id 过滤）；桌面 TaskList 搜索框 + 移动端搜索共用（6 测试） |
| R5 | 澄清超时提醒 | ✅ 每 10 分钟扫描 clarifying 超阈值任务（config `clarify_timeout_hours` 默认 24h），notify + `clarify_timeout` 广播，每任务仅提醒一次（2 测试） |
| R6 | 分级标准可配置 | ✅ config.yaml `grading: {heavy,light}` + `configureGrader()` 启动注入，追加内置规则（6 测试） |
| R7 | SSOT 集成测试 | ✅ 两节点 mock 任务：上游完成后 STATUS_REPORT 版本递增且下游 system prompt 拿到最新文档；篡改沙箱文档后 checkDocs 自动恢复（2 测试） |
| R8 | CLI 内置命令 | ✅ `server/src/cli.ts`：`coteam init <dir> [--name] [--description]` 调 scaffoldProject；bin `coteam-cli` + `npm run cli`（5 测试） |
| R9 | 结构化测试报告 | ✅ 修复循环终态 `result.report = {framework, attempts, failures, summary}`；达上限保留完整失败明细（含 escalation 后）；成功通过且经历修复轮时报告 attempts+零失败；桌面/移动 UI 展示 |
| R10 | 真实 LLM e2e | ✅ `scripts/e2e-demo.sh`（scaffold 建项目→建任务→轮询→快照/知识库/作战室摘要），`bash -n` 通过；**真实 key 全流程验收由用户在场执行** |

### 桌面聊天界面微信化

- ChatStream 全面重写：Agent 左侧（36px 圆角方头像按角色配色，主 Agent 蓝底"主"）/ 用户介入右侧绿气泡（#95EC69/暗色 #3eb575）+ CSS 三角尾巴 / 工具动作行居中灰系统消息 / >5 分钟才显示居中时间条 / 文件卡片 / typing 三点动画。**数据逻辑零改动**
- 新组件 `AgentAvatar.vue`（头像配色集中）+ `InterventionBar.vue`（微信输入条，嵌入作战室底部）
- JournalEntry kind 扩展 `'intervene'`；ChatReplay 继承新样式

### 独立移动端前端（与桌面完全解耦）

- **新工程 `mobile/`**：Vue3 + Vite + Vant 4 + vue-router（`createWebHistory('/m/')`），与 web 零代码共享、只共用后端 REST/WS
- **全界面微信风格（2026-09-05 追加，用户要求"完全按照微信风格"）**：
  - 全局微信色板与 Vant 主题变量全量覆盖：主绿 #07C160、危险红 #FA5151、警示橙 #FA9D3B、链接蓝 #10AEFF、页面底 #EDEDED、文字 #191919/#888/#B2B2B2；暗色对齐微信深色模式（#111/#191919）
  - 任务列表 = 微信**会话列表**心智：48px 圆角方头像（执行 Agent 角色色）+ 标题行/摘要行（"[执行中] 节点名 — agent"）+ 右上角会话时间（今天 HH:MM/昨天/M月D日）+ 红点与数字角标（待审批/需澄清）
  - Agent 总览 = 微信**通讯录**心智：分组标签（主 Agent/开发组/测试组…）+ 成员 cell（忙碌橙点、正在执行节点、模型徽标）
  - 任务详情 = 微信**聊天页**心智：导航标题"任务名(成员数)"、聊天区纯净 #EDEDED、Tab 改"聊天/执行详情/事件"；执行详情与事件改微信分组列表（圆角白卡 + hairline 分隔 + 右箭头）
  - 发起/澄清/计划确认 = 微信**设置页**心智：灰色分组标题 + inset 白卡 cell + 微信绿主按钮（.wx-btn/#07C160，按压 #06AD56）
  - 聊天流气泡绿 #95EC69（暗色 #3EB575）、发送条为微信输入栏样式；Tabbar 图标"任务=chat/成员=friends"
- 六屏：任务列表（搜索/下拉刷新/加载更多/审批）、任务详情（成员头像条+微信聊天流+底部介入输入条/执行详情/事件）、发起任务（目录级联 Picker）、澄清问答、计划确认、Agent 总览
- 移动端专用健壮性：WS 指数退避重连 + visibilitychange 回前台立即重连并补拉；`VITE_API_BASE` 钩子（后续 Capacitor 打 app 即绪）
- 后端：staticMiddleware 支持前缀挂载，`/m/` serve `mobile/dist`（`COTEAM_MOBILE_DIST` 可覆盖），同源零 CORS
- 访问：手机同 WiFi `http://<局域网IP>:8855/m/`；manifest 可添加到主屏幕；深色跟随系统
- **桌面 web 零响应式改动**（按用户要求两端完全解耦）

### 移动端项目功能（2026-09-05 追加，用户反馈"下发任务不能选项目、项目进度监控不到"）

- **Tabbar 新增「项目」页**：项目列表（微信 cell 心智：项目图标+进行中红点+任务/完成/问题统计+完成度进度条）
- **项目详情页**（进度监控）：统计头（任务/完成/执行中/失败四格+总进度条+「⚠ N 个任务需要你处理」提醒）+ 项目内任务列表（会话行心智，状态标签：执行中/待处理），点任务直达作战室；右上角 + 下发任务自动带入项目
- **发起任务支持选项目**：新增「所属项目」cell（不关联项目/项目选择器），选中后**自动带出项目 workspace**；从项目页进入时 query 预选
- 任务列表页项目任务显示项目名 tag
- **后端修复（Bug-5）**：`listProjects` 用 `project:*` 模糊匹配把 `project:xxx:memory`（项目记忆数组）也当项目返回，且无 shape 校验——历史污染数据直接顶掉项目列表（表现为接口返回无 id/name 的怪对象）。修复：过滤 `:memory`/`:deleted` 键 + 校验 `p.id/p.name` 才收录；修复后列表干净返回真实项目
- 验证：项目页渲染 2 项目、co-team 详情统计头+6 任务、项目下发预填（co-team + D:\pxx\co-team）、默认"不关联项目"；projectMode 测试回归通过

### Agent 技能系统 + 执行骨架 Harness（2026-09-05）

- **Harness 执行骨架**（`server/src/harness.ts`）：七层结构化 system prompt（身份/不可协商准则/协同上下文/工作流契约/工具策略+轮次预算/输出契约/升级语义）替换平面拼接；`validateAgentResult` schema 门控（summary/verification 必填、字段类型校验），违规走精准修复轮；AgentResult 新增 `verification` 字段
- **Skill 技能系统**（`server/src/skills.ts`）：SKILL.md 约定（frontmatter+正文），全局库 `skills/` + Agent 专属 `agents/<x>/skills/`；agent.yaml `skills:` 显式绑定全量注入，未绑定按 tags/关键词自动匹配（上限2）；API 五端点（CRUD+reload）；设置面板「技能库」tab + Agent 绑定多选；种子技能 code-review-checklist
- **节点交付成果**（`server/src/deliverable.ts`）：统一模板（做了什么/目标贡献/变更/验证/问题），三终态（完成/接管成功/失败）自动生成，KV+journal（kind `deliverable`）卡片，作战室点击阅读（双端弹窗渲染）
- **项目进度成果表**：`GET /api/projects/:id/report`（每任务×每节点完整记录+交付成果内嵌），OfficeView「进度成果表」对话框（任务行展开节点明细），移动端项目页「成果表」页
- 真实任务验证：m3wht3qe「分析一下当前项目的缺陷」——技能自动装载日志、deliverable journal 卡片、统一模板报告全部落地
- 回归：152 用例全绿

### 为后续路线留门（未实施）


- iOS/安卓 app：移动端工程 + `VITE_API_BASE` 就绪，Capacitor 打壳即可（需 HTTPS 或资源打包）
- 飞书 bot：notify 出方向已有；收消息需回调端点 + 公网 + **鉴权前置**（当前 API 裸奔，任何对外暴露前必须先加最小 token 门禁——红线）

---



| 优先级 | 问题 | 状态 |
|--------|------|------|
| P0 | Orchestrator 未使用 LangGraph StateGraph | ✅ 已重构：StateGraph + 条件门控 + reducer，保留线程驱动兜底 |
| P0 | 无 Sandbox，Agent 可操作任意文件 | ✅ 已实现任务级沙箱 + 路径逃逸防护 + 命令白名单 |
| P1 | MemoryBus 不持久化 | ✅ JSON 落盘（exit flush + close flush），重启恢复 |
| P1 | LLM 规划 edges 经常为空 | ✅ 已实现 fallback：无效边自动生成线性链 |
| P2 | Dashboard 无历史任务 | ✅ 任务列表持久化 + 状态实时同步 |
| P2 | 无 token 统计 | ✅ usage_metadata 统计 + 成本核算 + 预算控制 |
| 新增 | WS 在事件循环中同步阻塞迭代 pubsub（打开页面后 API 全挂） | ✅ 已修复：后台线程读取 + asyncio 队列 |

---

## 里程碑时间线

```
Week 1-2:  ✅ 基础可用，能跑通 demo
Week 3-4:  ✅ 执行引擎，Agent 能操作文件
Week 5-6:  ✅ 专业 Agent，各司其职
Week 7-8:  ✅ 智能调度，并行执行
Week 9-10: ✅ 监控通知，可观测
Week 11-12: ⏳ 产品化，pip install（打包发布待完成）
```

---

## 竞品参考

| 项目 | 定位 | Co-Team 差异 |
|------|------|-------------|
| Devin | 全自主 AI 工程师 | Co-Team 更轻量，个人开发者友好 |
| OpenHands | 单 Agent 开发 | Co-Team 多 Agent 协作 |
| AutoGen | 多 Agent 框架 | Co-Team 开箱即用，非框架 |
| Cursor | AI 编辑器 | Co-Team 独立运行，跨 IDE |
| Hermes Agent | 编排引擎 | Co-Team 内置模型池 + 插件系统 |

---

## 核心设计原则

1. **可见性优先** — 用户随时知道每个 Agent 在做什么
2. **渐进式复杂** — 简单任务 1 个 Agent，复杂任务自动拆解
3. **失败可控** — 任何 Agent 失败都有明确的恢复路径
4. **零配置** — 内嵌依赖，开箱即用
5. **插件化** — Agent、模型、通知渠道均可扩展

---

## 验证记录（2026-09-02）

- 单元/集成测试：`tests/` 4 个文件全部通过（router / scheduler+sandbox+tools / orchestrator（重试、升级、并行、审批、取消、上下文共享、持久化）/ transport 持久化）
- LangGraph 驱动专项验证：并行 fan-out/join、取消、审批挂起→恢复、失败级联取消下游
- 端到端实测（真实 LLM）：提交「创建 hello.py + pytest 测试」任务 → 自动规划 dev→test 两节点 → 沙箱内生成文件并通过测试 → 合并回工作区 → git 自动创建分支 `coteam/task-*` 并提交
- 服务冒烟：`/api/status`、`/api/agents`、`/api/metrics`、`/api/tasks`、`/docs`、`/`（仪表盘）全部 200

---

## 前后端全 TS monorepo 重构（2026-09-03）✅

原单文件 HTML 仪表盘 + Python 后端已重构为全 TypeScript monorepo，目标 `npm i -g co-team` 零 Python 依赖安装（对齐 hermes/openclaw 分发体验）。

| 模块 | 状态 |
|------|------|
| server/（Node + Hono + ioredis + openai SDK + simple-git） | ✅ 逻辑 1:1 照搬已验证的 Python 设计：并行 DAG、重试→接管→人工介入、审批、取消、沙箱、git、对话记录、模型降级/成本选型 |
| web/（Vue3 + Vite + Element Plus + ECharts） | ✅ 1:1 平移全部仪表盘功能（任务表单+目录选择器、agent 卡片、任务列表、事件日志、模型池、指标、对话回放、审批/取消） |
| 新功能：任务 DAG 拓扑图（ECharts 分层布局） | ✅ |
| 新功能：Agent 负载图 + 模型 Token 图表 | ✅ |
| vitest 测试（复刻 Python 套件，23 用例） | ✅ 全绿 |
| 真实 LLM 端到端 | ✅ 规划→沙箱→落盘→git 自动提交→对话记录/DAG 可见 |
| API 契约 | ✅ 与 Python 版逐字一致，调用方零迁移 |
| Python 原版 | 📦 移入 `legacy-python/` 存档 |
| npm 发布 | ⏳ bin/files 已配置，本地 `node server/dist/index.js` 可跑；待正式 publish |

常用命令：`npm run build`（构建全部）、`npm run dev:server` / `npm run dev:web`（开发）、`npm test`、`npm start`（启动服务，端口 8855 不变）。

---

## 十项改进落地（2026-09-04）✅

按《Co_team项目改进Agent执行任务文档.md》完成 10 项关键改进（安全沙箱强隔离按文档要求搁置，仅在 README 标注实验特性；Git 管理已补齐）。

| # | 改进项 | 实现 |
|---|--------|------|
| 1 | 项目初始化工作流 | `POST /api/projects` 支持 `scaffold: true`：docs/src/tests/config 目录 + README/CONTRIBUTING/ARCHITECTURE.md 模板生成 + git init/根提交；UI 新建项目可选 |
| 2 | Git 管理 | 项目创建自动 git init + 提交规范落 CONTRIBUTING 模板（分支工作流此前已完备） |
| 3 | 知识与经验沉淀 | 服务端统一目录（data/knowledge/{general-tech,projects/{id}}）Markdown+frontmatter 存储；标题去重更新；关键词检索；agent 经 `write_knowledge` 工具自主沉淀；任务结束复盘自动写入；planner/agent 提示词注入相关条目；UI 知识库管理面板（增删改查/搜索/编辑器） |
| 4 | 文档驱动协同 | SSOT 文档协议：TASK_SPEC/API_CONTRACT/STATUS_REPORT 版本号+哈希注册表，`doc_updated` 事件广播，执行前文档检查（缺失/篡改自动恢复），原子写入；**行为契约已补齐**：agent 经 `write_doc` 工具自主更新协同文档（版本注册表+作战室卡片，成果归属写入 upstreamContext），经 `send_message` 相互留言（延迟派发：收件方下次 dispatch 注入，orchestrator/user 留言在节点执行前落作战室，任务终态未送达消息标「未送达」归档）；docsSection 短文档全文注入提示词 |
| 5 | 需求理解与澄清 | LLM 需求清晰度评估（目标/约束/验收），不达标进入澄清循环（最多 3 轮），人类「确认」后才规划执行；UI 澄清对话框 |
| 6 | 双向进度沟通 | 统一进度消息（percent/ETA/当前节点）；`GET /api/tasks/{id}/progress` 查询；主动汇报 30s 节流防轰炸 |
| 7 | 任务分级处理 | light/standard/heavy 三级（规则+人工覆盖），分级映射计划模板（节点数/文档要求/评审节点）；UI 分级选择器 |
| 8 | 测试修复闭环 | 测试输出解析器（pytest/vitest/jest）；失败测试阻止提交，解析错误→定向修复→重测（轮次可配，默认 3）；每轮入作战室日志；超限上报人工 |
| 9 | 异构技能协同 | GLOBAL_GOAL 全局目标：执行时生成、注入每次 agent 调用、节点 goal_link 贡献声明；目标更新广播并失效 agent 会话；UI 查看/编辑 |
| 10 | 快照与回滚 | git 引用 + 协作状态 KV 导出的显式快照（task-start/decision/task-end/手动）；索引按任务/标签查询；回滚需确认+审计日志；沙箱硬重置/工作区建回滚分支；UI 快照列表+一键回滚 |
| 11 | 主 Agent 模型锁定 | 创建时手动指定（含模型池通断展示）即锁定，replan 沿用；故障允许降级但必留痕；中途可手动更换（API+UI，作战室留痕） |

新增模块：scaffold/templates、knowledge、clarify、grader、ssot、testloop、snapshot、progress；配置新增 `knowledge.dir`、`orchestrator.max_fix_rounds`。
测试：vitest 15 文件 76 用例全绿（新增 scaffold/knowledge/progress/ssot/snapshot/clarifyFlow/testFixLoop/clarifyGraderTestloop 8 个文件）。

---

## 群组沟通 → 方案转项目 → 经验沉淀（2026-09-08）✅

新增一条完整产品链路：**用户发起群组讨论（勾选 agent）→ 自主沟通打磨项目规划方案 → 一键转项目+开发任务 → 讨论期/开发期经验自动沉淀到项目并可查看**。

| 能力 | 实现 |
|------|------|
| 群组讨论引擎 | `server/src/discussion.ts`：讨论/消息全走 message bus KV（Redis/内存双支持）；每轮全员被唤起、各自输出 `{speak, reply, experience, ask_user}` 自决发言或沉默（无关 agent 不强制）；@ 点名成员必须发言（speak:false 时定向追加重试一次）；LLM 调用失败/输出不可解析一律诚实沉默，绝不编造发言；轮末系统消息登记发言/沉默/提问名单 |
| 双模式节奏 | 手动（每条用户消息触发一轮 + 「继续讨论」自由轮）/ 自动（主持判定收敛，单次上限 3 轮，可随时停止）；详情页随时切换；并发锁 busy（10 分钟 TTL）+ 启动清扫 clearStaleDiscussionLocks（仿 E7 僵尸任务清扫） |
| 用户=决策方 | 用户发言标注「决策方」注入每个成员上下文，补充与方向性修正有约束力；agent 遇方向取舍/资源决策/重大分歧用 `ask_user` 提问（@你 待拍板高亮，回复后派生「已回复」）；自动循环检测到待回答立即暂停；桌面通知 + 导航徽标 |
| 方案生成 | 主持人 LLM 收敛讨论为结构化《项目规划方案》markdown（背景/范围/技术方案/里程碑/风险/经验决策摘要/待定事项=未回答提问）；可重新生成、可人工编辑（版本 +1） |
| 方案转项目 | 新建项目（scaffold/git init）或挂入已有项目；讨论即澄清（任务 skipClarification + planAsync）；方案全文+来源标注+待定事项作为任务描述；方案沉淀项目知识库（category=project，source=discussion:{id}）+ 项目记忆；auto_run 时轮询 planned 后进队列 |
| 经验沉淀 | 成员发言携带 experience → writeKnowledge（挂项目则项目归类，tags=[群组讨论, agent]，source 标注）+ agent 个人记忆；开发期沿用既有任务复盘自动沉淀；GET /api/knowledge 新增 source 前缀过滤 |
| API | `/api/discussions` CRUD + messages/round/stop/scheme/convert；新事件 discussion_* 走 emitProgress（/ws/events 零改动广播） |
| Web | 第 4 个导航 tab「群组沟通」（待拍板徽标）：GroupDiscussionView/DiscussionChat（微信气泡+ask_user 高亮+@chip 点名+打字指示器）/SchemePanel/ConvertDialog；OfficeView 新增「项目经验·知识库」区块 |
| Mobile | tabbar 新增「沟通」（徽标）：DiscussionListView/DiscussionDetailView（同款气泡+@chip+方案/转项目/经验弹层，marked 渲染）；ProjectDetailView 新增项目经验区块——双端同步达成 |

测试：`server/test/discussion.test.ts` 22 用例（自决沉默名单/@强制/ask_user 落库+自动暂停/待回答派生/诚实降级/busy 409/方案生成含待定事项/convert 建项目+建任务+沉淀/auto_run 进队列）；全量 40 文件 250 用例绿；三端 build 通过。
GUI 实拍（8856/8857，gui-test-screenshots/discussion-*）：真实 LLM 全链路——发起→3 轮自主讨论（test 自决补充、dev 被 @ 强制发言并 ask_user、用户拍板续聊）→方案 v1→转项目（脚手架+知识库条目+任务 17 节点 planned）→任务详情联动；测试产物已清理。
